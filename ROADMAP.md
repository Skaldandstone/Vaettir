# Vaettir Roadmap

This is the working backlog for the platform beyond the initial scaffold
(see [README.md](README.md) for what's already built: core data model, AI
reverse-engineering MVP, web/mobile/api scaffolding).

Structure: **Phase → Epic → Ticket**. Each ticket has an id (stable enough to
reference in commits/PRs before it exists in Linear), a size estimate
(S = ~1 day, M = ~2-3 days, L = ~1 week, XL = needs breaking down further
before starting), and suggested Linear labels. A machine-readable version of
every ticket below is in [roadmap.csv](roadmap.csv) for bulk import into
Linear (Settings → Import/Export → CSV).

Suggested Linear project-per-phase, with each Epic as a Linear
sub-project/milestone or a label - whichever your workspace convention
prefers.

---

## Phase 0 - Foundations (done)

Shipped in the initial scaffold: monorepo (pnpm/Turborepo), Prisma schema
with pluggable `TestPlanType`/`ComplianceFramework`, BDD-normalized
`TestCase` model, Fastify+tRPC API, Next.js web shell, Expo mobile shell,
AI reverse-engineering MVP (single-file paste → BDD test cases), CI
typecheck workflow. Not re-listed here - see git history.

---

## Phase 1 - Platform Foundations

Auth, multi-tenancy, and hardening the CRUD surface the AI/dashboard
features will sit on top of. Nothing else is safely multi-user without this.

### Epic 1.1 - Auth & Identity
- ✅ **P1-01** (M) Add authentication (NextAuth or Clerk) with email/password + at least one SSO provider (Google Workspace). `labels: area:api, area:web, type:feature` - done via Clerk; SSO provider config is an org-by-org Clerk dashboard setting, not code (see P1-04 for enterprise SAML specifically).
- ✅ **P1-02** (M) Session/JWT handling in the tRPC context; replace `publicProcedure` with `protectedProcedure` across all routers. `labels: area:api, type:security`
- ✅ **P1-03** (L) Role-based access control model: `Owner`, `Admin`, `Editor`, `Viewer`, `ComplianceAuditor` roles scoped per Organization/Project. `labels: area:api, area:db, type:feature`
- **P1-04** (M) SSO/SAML support for enterprise orgs (compliance customers will require this). `labels: area:api, type:feature, compliance` - needs a Clerk Enterprise SSO/SAML connection configured per customer; not started.
- ✅ **P1-05** (S) API keys / service tokens for CI integrations (test result ingestion, PR scanning) that don't go through a human session. `labels: area:api, type:feature`

### Epic 1.2 - Multi-tenancy hardening
- ✅ **P1-06** (M) Enforce `organizationId`/`projectId` scoping in every query via a Prisma middleware or repository layer - audit the existing routers, which currently trust client-supplied `projectId`. `labels: area:api, type:security` - done via `requireProjectAccess`/`requireOrgRole` on every protected procedure, not a Prisma middleware; verified live that a cross-project id is correctly rejected, not just silently trusted.
- ✅ **P1-07** (S) Add `createdById`/`updatedById` audit columns to core models (`TestCase`, `TestPlan`, `Release`, `RiskFlag`). `labels: area:db`
- ✅ **P1-08** (M) Org/project settings UI: members, roles, project repo linking. `labels: area:web, type:feature`
- ✅ **P1-09** (S) Seed script for a demo org/project so new devs and CI have consistent fixture data instead of manually pasting project ids. `labels: dx`

### Epic 1.3 - Core CRUD UI hardening
- ✅ **P1-10** (M) Test case create/edit forms in web, supporting **both authoring formats**: BDD (given/when/then) and the structured step table (`TestCaseStep` - action / expected action-or-data / expected result / expected response, with org-configurable field labels via `Organization.stepFieldLabels`). A test case needs at least one format populated, not necessarily both. `labels: area:web, type:feature`
- ✅ **P1-16** (S) Step field label settings: org settings UI for renaming the four `TestCaseStep` field labels (stored in `Organization.stepFieldLabels`) - the default names ("Test Step", "Expected Action / Data", "Expected Result", "Expected Response") are explicitly placeholders; this is what lets a team make them their own without waiting on a code change. `labels: area:web, type:feature`
- ✅ **P1-17** (M) Test-case-level version history. Found via the 2026-08-27 competitor parity audit: `P4-05` versions a `TestPlan`, but not the individual `TestCase`s inside it - every competitor researched versions the case itself. `labels: area:api, area:web, type:feature` — new `TestCaseVersion` model + `services/testCaseVersion.ts`'s `snapshotTestCaseVersion`, exact same "sequential per parent, computed from the max existing version" pattern as `P4-05`'s `TestPlanVersion`. Called after every real create/update/CSV-import path (`testCases.create`/`update`/`importCsv`), storing a full frozen snapshot (title, both authoring formats' content, tags, priority, type) - `steps` is a JSON snapshot rather than a live relation, since structured steps don't carry stable identity across an edit (replace-all on update, per the existing comment on `update`). New `testCases.history` query and a "History" section on `TestCaseDetailContent` (mirrors `TestPlanDetailContent`'s `VersionHistorySection` - client-side diff against the prior snapshot, no server-side diff storage). Verified for real against the live local dev database: created a case (exactly 1 version), edited it twice (versions 2 and 3 correctly sequential, correct diffs), and confirmed version 1's snapshot still showed its original `then` content unchanged after later edits - proving each version is a genuine frozen point-in-time record, not a live reference. All synthetic data deleted after verification.
- ✅ **P1-18** (M) Shared/reusable step libraries. Found via the 2026-08-27 competitor parity audit: TestRail, qTest, Zephyr, and Xray all let a step sequence be authored once and reused across many test cases. `labels: area:api, area:web, type:feature` — new `SharedStepGroup` model (project-scoped, a JSON steps array in the same shape `TestCaseStep` uses) and `TestCase.sharedStepGroupId`. A case either owns its structured steps or defers entirely to a linked group, never both - the same "one or the other" simplicity `given`/`when`/`then` vs. structured steps already has. Resolution happens live at read time (`testCases.byId`, `manualExecution.getForExecution`) rather than copying the group's content onto the case, so editing a shared group instantly updates every case using it. New `sharedStepGroups` router (list/create/update/delete - delete refused while any case still references it, with the exact usage count in the error) and a `/shared-steps` management page; `TestCaseForm` gets a picker that swaps the manual step editor for a read-only preview once a library is selected. Verified for real against the live local dev database: created a group, linked a real case to it, confirmed the case's resolved steps matched the group exactly, edited the *group* (not the case) and confirmed the case's resolved steps updated to 4 steps with zero changes to the case itself, confirmed `usageCount` tracked correctly, confirmed deletion was correctly refused while in use, and confirmed unlinking the case correctly cleared both `sharedStepGroupId` and its resolved steps before deletion succeeded. All synthetic data deleted after verification.
- ✅ **P4-07** (M) AI test case generation from a requirement/user story. Found via the 2026-08-27 competitor parity audit as the highest-value AI gap: every researched competitor (qTest Copilot, Xray AI Test Case Generation, TestRail's Sembi IQ, Zephyr's HaloAI/BearQ, PractiTest's SmartFox) now ships this - Vaettir's existing AI investment only went the reverse direction (source code → test cases, `P2-*`) and into strategy drafting (`P4-02`). `labels: area:ai, type:feature` — new `@vaettir/ai-agent` module `generateTestCasesFromRequirement.ts` (same tool-use pattern and defensive `coerceStringArray` handling as `P4-02`'s `generateStrategy.ts`) plus `requirements.generateTestCases`, metered at 8 AI credits. Draft-only, same review-before-save shape as every other AI feature in this codebase (`P2-06`, `P4-02`, `P5-12`) - nothing is persisted until the user explicitly picks which drafts to keep via the normal `testCases.create` path, so an accepted draft lands `AUTHORED`, not gated behind the AI-reverse-engineering review queue (correct: by the time a human has read, could edit, and chose to commit it, it's no different from typing it in themselves). New "Generate test cases with AI" button per requirement on the Requirements page, opening a checklist review panel. Verified for real against the live Kall project with a genuine Anthropic call: gave it a real, detailed requirement (password reset via email link, with expiry/one-time-use/session-invalidation rules) and got back 8 specific, correctly-prioritized test cases that actually covered link expiry, reuse-after-success, session invalidation across devices, email-enumeration protection, tampered tokens, password-validation failure, and superseded-link invalidation - genuinely grounded in the requirement's specifics, not generic boilerplate. Confirmed the AI credit ledger charged exactly 8, and confirmed a committed draft round-tripped through the real `testCases.create`/`byId` path with the correct `AUTHORED` origin. All synthetic data deleted after verification.
- ✅ **P4-08** (M) Parse requirements from a markdown spec doc or a connected GitHub repo, not just manual entry. Direct user request (2026-08-28). `labels: area:ai, area:web, type:feature` — new `@vaettir/ai-agent` module `extractRequirementsFromMarkdown.ts` (AI call, not a structural parser - unlike Gherkin/Postman/CSV, prose requirements docs have no fixed syntax; explicitly instructed to skip non-requirement noise like changelogs/license text/install steps and return an empty list rather than invent requirements when a doc has none). New `services/repoDocScan.ts` (shallow-clone-and-walk, same shape as `repoScan.ts`'s test-file scanning) narrows a repo scan to README + `docs/`/`spec/`/`requirements/`-hinted paths rather than every markdown file, since most of a real repo's markdown is noise for this purpose; capped at 10 files. New `requirements.extractFromMarkdown` (paste or upload a `.md` file) and `requirements.extractFromRepo` (uses the project's connected repo by default), each charging AI credits per document actually scanned - a repo scan that finds nothing costs nothing, one that finds 10 docs costs 10x a single paste. Same draft-and-review shape as every other AI feature (`P2-06`, `P4-02`, `P4-07`) - nothing is persisted until the user picks which drafts to keep via the normal `requirements.create` path. New "Extract from markdown file" and "Extract from repo" buttons on the Requirements page. Verified for real, no shortcuts: fed a real markdown doc with 3 requirement sections plus a changelog section through `extractFromMarkdown` with a genuine Anthropic call - came back with 5 correctly-granular requirements (the AI split one section into two genuinely distinct, individually-testable requirements) and correctly excluded the changelog entirely; confirmed the AI credit charge was exactly 4; committed one draft through the real `requirements.create` path. Then ran `extractFromRepo` against Vaettir's own real, public GitHub repo - a genuine `git clone` and scan, not a fixture - and confirmed it correctly found and extracted from `README.md` and `docs/AWS_DEPLOYMENT.md` (63 requirement drafts across both real files).
- ✅ **P1-11** (M) Test plan create/edit UI, including dynamic form rendering from `TestPlanType.fieldSchema` (JSON Schema → form). `labels: area:web, type:feature`
- ✅ **P1-12** (S) Requirement + AcceptanceCriterion CRUD UI, linkable to test plans. `labels: area:web, type:feature`
- ✅ **P1-13** (M) Bulk operations: tag test cases, bulk move between test plans, bulk archive. `labels: area:web, type:feature`
- ✅ **P1-14** (S) Global search across test cases/plans (Postgres full-text to start; revisit if scale demands a search index). `labels: area:api, area:web` - done via case-insensitive substring match; full-text is the noted future upgrade once relevance ranking matters.
- 🟡 **P1-15** (M) Replace ad-hoc `useEffect` fetch patterns in web with `@trpc/react-query` for caching/invalidation (current pages do manual state, fine for a scaffold, not for a real app). `labels: area:web, type:tech-debt` — started with James actively in the session (2026-09-05), same reasoning `P10-15`'s ESLint rollout used: a broad-blast-radius change across every page is exactly what deserves a live human around, not unattended overnight work. New `lib/trpcReact.tsx` adds a second, hooks-based client (`createTRPCReact`) alongside the original `lib/trpc.ts` proxy client - deliberately NOT touched or removed, so all 27 not-yet-migrated pages keep working completely unchanged; `getAuthHeaders` (the Clerk-token/race-condition logic) is exported from the original file and shared by both, so there's exactly one copy of that logic, not two that could drift. `<TRPCReactProvider>` wraps the whole app in `app/layout.tsx`. Two real pages migrated end-to-end so far: `/settings/members` (queries, mutations, and a modal) now uses `useQuery`/`useMutation` with `utils.organization.invalidate()` after every mutation instead of a hand-written `loadOrgData()` refetch-everything function; `/projects` (create/edit/delete plus a one-off imperative lookup - `startEdit`'s fetch of a project's `defaultBranch` before opening the edit modal, replaced with `utils.project.byId.fetch()`, react-query's idiomatic one-shot-imperative-call equivalent, not a rendered `useQuery`). Both verified for real in the browser, not just typechecked: on `/settings/members`, sent a real invite and watched "Pending invitations" update live with zero manual refetch code, then revoked it and watched it disappear the same way; on `/projects`, ran the full real create → edit (including the imperative `byId` prefetch correctly populating the edit modal's default-branch field) → delete cycle and watched the list update live after each step with no manual refetch anywhere. A third, simpler page also migrated: `/projects/[projectId]/audit-log` (a single filtered read query, `entityType` as a `useQuery` input rather than a manually re-triggered `useEffect`) - verified live, including clicking each entity-type filter button and confirming the table correctly re-queried and re-rendered (down to a real, correctly-empty "No activity recorded yet" for a filter with no matching rows). Spot-checked that unmigrated pages (test-cases, dashboard) still render correctly under the new provider, and confirmed production health/the migrated pages loading correctly after each deploy. **Update 2026-09-10: every page is migrated.** All 29 data-fetching pages under `apps/web/app` (the other 5 `page.tsx` files have no tRPC calls) now import from `lib/trpcReact`; zero pages import the old `lib/trpc` proxy client. Pattern details worth knowing for the next reader: `useReadOnlySeat(projectId)` in `lib/trpcReact.tsx` replaces the copy-pasted project→org→seatType effect chain; mount-fired AI mutations (requirements' generate/extract modals) go through `utils.client.<proc>.mutate` so the effect deps stay minimal; on-demand reads against transient input (CSV previews, webhook deliveries, hard-delete preview, edit-modal prefetches) use `utils.<proc>.fetch()`; background job lists poll via `refetchInterval` only while a job is PENDING/RUNNING; editable settings forms are seeded from the query row exactly once so background refetches never clobber unsaved edits; four procedures gained bounded `.output()` schemas (`organization.bootstrap`, `testCases.approve`/`reject`, `testPlans.create`) because a raw Prisma return type through react-query's `useMutation` wrapper hits TS2589. **Honest evidence tier for the 2026-09-08..10 batches (26 pages): web typecheck clean + api tests passing + line-by-line diff review, NOT browser-verified** - the dev session was signed out after the Clerk v7 bump and no demo credentials exist, so unlike the first three pages these have not been clicked through; the first real sign-in after deploy should walk the highest-traffic pages (test-cases, test-runs, dashboard, settings/organization, import). **Still on the old client: 5 shared components** (`GlobalSearch`, `Sidebar`, `TestCaseDetailContent`, `TestCaseForm`, `TestPlanDetailContent`, ~2050 lines) - `lib/trpc.ts` stays until those move.

---

## Phase 2 - AI Reverse-Engineering at Scale

The MVP does one pasted file at a time, synchronously. This phase makes it a
real ingestion pipeline.

### Epic 2.1 - Repo-wide scanning
- ✅ **P2-01** (L) `ReverseEngineerJob` worker: background queue (BullMQ + Redis, or a simple Postgres-backed job table to start) processing `REPO_SCAN` jobs file-by-file. `labels: area:api, type:feature` - Postgres-backed (in-process poller), not BullMQ/Redis; matches the "start simple" framing, revisit only if it needs to scale beyond one API process.
- ✅ **P2-02** (M) GitHub repo connector: clone/checkout a repo (or use the Contents API) given a `Project.repoUrl`, walk test-file glob patterns per detected framework. `labels: area:api, type:feature, integration:github`
- ✅ **P2-03** (S) Job status UI: progress bar, per-file results, retry-failed-files. `labels: area:web, type:feature`
- ✅ **P2-04** (M) Deduplication: re-running a scan on an unchanged file should not create duplicate `TestCase`s - key on `(filePath, functionName)` + content hash, update in place if the source changed. `labels: area:api, type:feature`
- ✅ **P2-05** (M) Diff-aware re-scan: on a new commit, only re-run the agent on changed test files (uses the same file-list logic Phase 6's PR scanner needs - build this once, share it). `labels: area:api, type:feature` - `changeImpact.ts`'s clone/resolve-ref logic is shared, not duplicated, exactly as this ticket asked.

### Epic 2.2 - Quality & feedback loop
- ✅ **P2-06** (M) Human review workflow for AI-reverse-engineered test cases: `PENDING_REVIEW` → `APPROVED`/`REJECTED` status, diff view (AI output vs. edited-by-human). `labels: area:web, area:api, type:feature`
- ✅ **P2-07** (M) Edit-and-resubmit: when a human edits an AI-generated test case, capture that as a labeled example; periodically review edit patterns to tighten the system prompt. `labels: area:ai, type:feature` — new `AiEditFeedback` model captures a before/after snapshot (title + BDD steps) whenever a human edits a case whose `origin` is `AI_REVERSE_ENGINEERED` *and* the BDD content actually changed - an edit that only touches priority/tags/suite, or an edit to an `AUTHORED`/`IMPORTED` case, isn't AI-quality signal and doesn't get captured. New "AI edit feedback" section on the reverse-engineer page shows the before/after side by side. The "periodically review edit patterns to tighten the prompt" half is inherently a human judgment call, not something to automate - this ships the capture + a real place to look at it, not an auto-tuning loop. Verified for real against the live Kall project: a content edit on an AI-origin case was captured with the exact correct before/after snapshot, a priority-only edit on the same case correctly did NOT capture (still exactly one feedback row), and a content edit on an `AUTHORED` case correctly did NOT capture at all. All synthetic data deleted after verification.
- ✅ **P2-08** (S) Confidence-based triage: auto-surface all test cases below a configurable confidence threshold into a review queue. `labels: area:web, type:feature` - the review queue (`testCases.pendingReview`) already surfaces every AI case awaiting a decision, sorted lowest-confidence-first; no separate numeric cutoff exists to configure, but the triage-ordering intent is met.
- **P2-09** (M) Batch cost/latency controls: chunk large files, cap concurrent agent calls, add per-org rate limiting on reverse-engineering jobs. `labels: area:ai, type:infra` - per-org rate limiting done (200 jobs/hour, rolling window); concurrent-call capping already existed (the worker processes one job at a time, globally); chunking large files is still not built (they're currently skipped outright above `MAX_FILE_BYTES`, not chunked and processed). Investigated for real this session (2026-08-28) before deciding not to build it yet: the natural, safe chunking unit already exists (the known-framework evaluators' deterministic `extract()` test-block list `reverseEngineer.ts`'s `buildPromptContent` already uses), so splitting a large file's blocks across multiple batched Anthropic calls inside `reverseEngineerTestFile` itself is straightforward and wouldn't need to touch `repoScan.ts`/`zipScan.ts`'s discovery-time `MAX_FILE_BYTES` skip at all (that skip is read-time, before framework detection is even possible, and is also a legitimate abuse/resource guard against reading huge files off an arbitrary cloned repo - not purely a cost optimization to just remove). The real blocker: `AI_OPERATION_COSTS.reverseEngineerTestFile` charges a flat 6 credits once per job regardless of size, charged *before* the AI call runs (the same charge-then-spend pattern every AI operation in this codebase uses) - internally splitting one job into N real Anthropic calls without charging proportionally would silently undercharge relative to actual spend on exactly the files this ticket targets (the largest ones). Charging correctly needs either knowing the chunk count before the flat charge (not available yet at that point in the call chain) or restructuring the whole charge-before pattern - a real pricing/architecture decision in the same sensitive territory `P12-12` is already flagged for, not a pure engineering task. Left unbuilt rather than shipping a feature that quietly undercharges large files.
- ✅ **P2-10** (S) Prompt regression test suite: a fixed set of sample test files (one per known framework) with expected BDD output, run in CI to catch prompt-change regressions. `labels: area:ai, type:testing` - *(delightfully recursive: this is a test suite for the thing that writes test cases)* — `packages/ai-agent/scripts/promptRegression.ts` (`pnpm --filter @vaettir/ai-agent test:prompts`), three real fixtures (Jest, pytest, JUnit) run against the actual `reverseEngineerTestFile` with real Anthropic calls. Deliberately NOT wired into the default `pnpm test`/CI push pipeline (`P10-10`/`P10-11`) and NOT exact-output snapshot assertions - LLM output isn't stable enough for snapshots, and every run costs a real API call, so it checks structural/content signals instead (extracted case count in an expected range, no empty BDD steps, expected keywords present somewhere in the extraction) and is meant to be run manually after touching the reverse-engineering system prompt, not on every commit. Verified for real: all three fixtures pass against the live system prompt today.
- ✅ **P2-11** (M) AI test case quality review: every AI feature built so far either generates cases (`P4-07`) or reverse-engineers them (`P2-*`) - nothing scanned *existing, already-authored* cases for real quality problems. New `@vaettir/ai-agent` module `reviewTestCaseQuality.ts` takes a whole test plan's cases in one batch call (not per-case) specifically so it can catch near-duplicate coverage *between* cases, which a one-at-a-time pass could never see - flags vague titles, unclear given/when/then or step sequences, when/action steps with no corresponding expected result, and genuine duplicate-coverage groups. New `testCases.reviewPlanQuality` mutation (capped at 30 cases per call, `truncated` flag surfaces when a plan has more) and `testCases.listForPlan` (a real gap found while building this - no procedure anywhere queried `TestCase` scoped to a `testPlanId` before now), metered at 12 AI credits. Purely diagnostic, same as every other AI feature here - findings are surfaced for a human to act on, nothing is auto-edited. New "Test case quality" section on the test plan detail page (`TestPlanDetailContent.tsx`, which previously had no test-case list at all). Verified for real against the live Kall project's "Submission Pipeline Readiness" plan (3 real, already-decent cases): correctly flagged exactly one genuine issue (a title/assertion mismatch) and one real near-duplicate precondition overlap, without padding the other two clean cases with manufactured findings - system-prompt instruction to only flag genuine problems held up on real, non-cherry-picked data. Separately proved positive detection with a deliberately bad throwaway pair (a vague `"test login"` title next to a true content-identical duplicate) in a browser end-to-end: correctly flagged the vague title with a specific rewrite suggestion and correctly identified the duplicate pair with an accurate reason. Confirmed the AI credit ledger charged exactly 12, and confirmed the empty-plan guard (`BAD_REQUEST` when a plan has zero cases) throws correctly. All synthetic data deleted after verification.

### Epic 2.3 - Multi-format input
- ✅ **P2-11** (M) File upload support (not just paste) - zip of a test directory, single file upload. `labels: area:web, type:feature`
- ✅ **P2-12** (S) Postman/Newman collection import → BDD (structurally different from code-based frameworks: request/assertion pairs, not functions). `labels: area:ai, type:feature` - extracts each request's `pm.test(...)` assertions; a request with no test script is skipped.
- ✅ **P2-13** (M) Gherkin/`.feature` file import - these are already BDD; this path validates/normalizes into the schema rather than inferring from scratch, and should reuse far less of the agent. `labels: area:ai, type:feature` - plain parser, zero LLM calls, exactly as scoped.

---

## Phase 3 - Compliance & Governance

This is the platform's differentiator per your original brief: don't
shoehorn compliance into generic test plans.

### Epic 3.1 - Framework & control management
- ✅ **P3-01** (M) Compliance framework/control admin UI: browse seeded frameworks (SOC 2, HIPAA, PCI DSS, GDPR, ISO 27001), add custom frameworks/controls. `labels: area:web, compliance`
- ✅ **P3-02** (L) Import well-known control sets from structured sources (AICPA TSC for SOC 2, NIST CSF, etc.) rather than hand-seeding - build an importer, not just static seed data, since these get revised. `labels: area:api, compliance` — shipped as `compliance.importControls` (bulk `createMany` with `skipDuplicates` keyed on `(frameworkId, code)`, so re-running an updated import is safe and never silently overwrites a control someone's already mapped test cases to) plus a CSV paste-and-import modal on the compliance page. Deliberately does NOT hand-seed or hardcode the actual text of AICPA TSC / NIST CSF / any published standard - this codebase reproducing (and inevitably going stale on) real audit-standard content is a correctness risk of its own, and defeats the ticket's own point about hand-seeding going stale. Instead an admin exports their control set (from AICPA's own materials, a NIST CSF spreadsheet, a vendor's list) to CSV and imports it here. Verified against live Kall data: fresh import created 3 controls, re-importing the same codes plus one new one correctly skipped the 3 existing and created only the new one, a changed title on a re-imported duplicate did NOT overwrite the existing row, and the CSV parser correctly round-tripped a quoted field with an embedded comma and an escaped quote.
- ✅ **P3-03** (M) Test case ↔ control mapping UI (many-to-many, already modeled via `TestCaseComplianceControl`) with coverage-per-control view: "which controls have zero mapped test cases." `labels: area:web, compliance`
- ✅ **P3-04** (S) Control coverage report export (PDF/CSV) for auditor handoff. `labels: area:web, compliance, type:feature` — shipped CSV export (`compliance.exportReport` + a client-side CSV button on the compliance page). Lists every control's mapped test cases with review status, and flags zero-mapped controls as gaps in a dedicated column. PDF was skipped: CSV is what most auditors actually want to import into their own tooling, and adding a PDF renderer is a real dependency for marginal benefit - can revisit if requested. Verified against live Kall data: mapped control correctly listed the test case + status, unmapped control came back with an empty array (the gap case).

### Epic 3.2 - Evidence & audit trail
- ✅ **P3-05** (L) Evidence attachment model: link a `TestResult` (a passing execution) as evidence for a control, with immutable timestamped record. `labels: area:db, area:api, compliance` — originally deferred (noted below, before this update) since Phase 5's execution ingestion didn't exist yet to link against; revisited now that it does. New `ComplianceEvidence` model, distinct from the existing `TestCaseComplianceControl` mapping: that one is a structural claim ("this case is designed to verify this control"), this is a dated proof point ("this specific execution passed, on this date"), optionally tied to a real `TestResult` or standing alone as a manual attestation. No update/delete mutation exists anywhere in the API for this model, by design - a wrong record gets superseded by a new one, never edited. New `compliance.recordEvidence`/`listEvidence`/`mappedTestCases`, each recording writes an `AuditLog` entry. New "Evidence & sign-off" drawer on the compliance page's control list.
- ✅ **P3-06** (M) Full audit log (who changed what test case/plan/control mapping, when) - required for most compliance frameworks' own controls around change management. `labels: area:api, area:db, compliance` — shipped as a new append-only `AuditLog` model (no update/delete mutation exposed - an editable audit log defeats its own purpose) with a `recordAudit()` helper called from test case create/quickCreate/update/delete/approve/reject, test plan create/update, and compliance mapTestCase/unmapTestCase. A new project-scoped Audit Log page lists entries reverse-chronologically with an entity-type filter. Scoped to the mutations an auditor actually asks about rather than every mutation in the app; skipped `createFramework`/`createControl` since those are global shared reference data with no project/org to attach the log entry to, not something the "who changed this project's stuff" trail is about. Verified against live Kall data: create/edit/approve/map/unmap/delete on a real test case each produced the correct entry in the correct order, and the entity-type filter correctly isolated the mapping events.
- ✅ **P3-07** (M) Sign-off workflow: a `ComplianceAuditor` role can formally approve a control's evidence for a given period/release, with a durable signed record. `labels: area:web, area:api, compliance` — new `ComplianceSignOff` model (period + free-text attestation statement, also immutable/no update-delete). Gated by actual role, not project access level - `COMPLIANCE_AUDITOR` ranks below `EDITOR` in this app's general permission hierarchy (`trpc.ts`'s `ROLE_RANK`), so `requireProjectAccess`'s rank-based `minRole` check can't express "must hold this specific role"; `signOffControl` does an explicit `membership.role` check instead, allowing `COMPLIANCE_AUDITOR`, `ADMIN`, or `OWNER`. Same evidence/sign-off drawer as `P3-05`. Verified for real against the live Kall project: mapped a real test case to a synthetic control, recorded evidence against it, confirmed an `EDITOR` is correctly rejected from signing off with the specific role-requirement message, confirmed a real `COMPLIANCE_AUDITOR` membership can sign off, and confirmed both actions wrote real `AuditLog` rows. All synthetic data (including a temporary auditor test user) deleted after verification.
- ✅ **P3-08** (S) Retention policy configuration per org (how long evidence/audit logs are kept), since different frameworks mandate different retention windows. `labels: area:api, compliance` — the `Organization.dataRetentionYears` field already existed in the schema (default 5) but had no way to change it; shipped `organization.updateDataRetention` (ADMIN-only, 1-20 year range) plus a "Data retention" control on the organization settings page. This only records the org's configured policy - the actual purge/archive job is `P12-08`, not yet built, so changing this has no immediate effect on stored data today; the UI says so explicitly. Verified against live Kall data: read the existing value (5), updated it to 7, confirmed the read-back reflected it, confirmed a request for 50 years was correctly rejected by the range validation, then restored the original value.

### Epic 3.3 - Custom compliance plan types
- ✅ **P3-09** (M) "Add your own compliance form" flow: org admin defines a new `TestPlanType` (category `COMPLIANCE`) with a custom `fieldSchema` via a form builder, no code change required - this is the feature that proves the "don't shoehorn" design decision actually works end-to-end. `labels: area:web, area:api, compliance, type:feature` — shipped as `testPlans.createType` (builder-friendly `fields: [{key, type}]` list, compiled server-side into the JSON Schema `fieldSchema` the existing `CustomFieldsForm` renderer already reads) plus a "Custom plan types" section on the organization settings page. Not project-scoped, matching `ComplianceFramework`/`ComplianceControl` - it's shared reference data, not something one project owns. Verified against live Kall data end-to-end: built a real "Vendor Security Review" compliance plan type through the builder, authored an actual `TestPlan` of that type in the Kall project with real field values, read it back through `testPlans.byId`, and confirmed the values round-tripped correctly and the new type appears (unfiltered) in the project's own "new test plan" type picker - no code change needed anywhere in that path. Browser verification wasn't possible (no authenticated Clerk session in a fresh preview tab); relied on the live-data backend check plus a full code read-through instead.
- ✅ **P3-10** (S) Template library: starter field schemas for common internal audit / vendor-security-questionnaire style plans, so "custom" doesn't mean "start from a blank JSON Schema." `labels: area:web, compliance` — shipped as a "Start from a template" picker on top of P3-09's builder form: Vendor Security Questionnaire, Internal Audit Checklist, and Change Management Review, each a starter field list that pre-fills the form (still fully editable, still goes through the same `createType` mutation - no new API surface). Purely client-side data, so verified by running all three templates' exact field lists through `createType` against the live Kall org and confirming each produced a plan type whose `fieldSchema.properties` exactly matched the template's field keys.

---

## Phase 4 - QA Testing Strategy Generation

Structured strategy plans (scope, risk areas, environments, entry/exit
criteria - the Testlio-style structure referenced in the original brief).
`qa-strategy` plan type already exists in the schema; this phase builds
generation + the UI around it.

- ✅ **P4-01** (M) Strategy plan authoring UI rendering the `qa-strategy` field schema (risk areas, environments, entry/exit criteria) as a structured, guided form rather than raw JSON. `labels: area:web, type:feature` — shipped as a dedicated `QaStrategyForm` in `TestPlanDetailContent.tsx`, swapped in (by `testPlanType.key === "qa-strategy"`) in place of the generic `CustomFieldsForm`'s single comma-separated text input. Each of the four fields (risk areas, environments, entry criteria, exit criteria) gets its own labeled section with a one-line hint on what belongs there, and a real add/remove row-list editor instead of a comma-separated string. Every other plan type (built-in or custom, per P3-09) still uses the generic renderer - this one earns a dedicated form because the roadmap calls it out by name as the fields a QA lead should actually sit and think through. Verified the data model against live Kall data: authored a real qa-strategy TestPlan with all four fields populated, read it back, and confirmed every field round-tripped exactly. Browser verification of the new UI itself wasn't possible (no authenticated Clerk session in a fresh preview tab); relied on a careful code read-through plus the above data-level check instead.
- ✅ **P4-02** (L) AI-assisted strategy generation: given a project's codebase summary (languages, frameworks detected, existing test coverage) plus a short prompt from the user ("we're shipping payments in Q3"), draft a starter strategy plan. `labels: area:ai, type:feature` — shipped as `generateQaStrategyDraft` (new `@vaettir/ai-agent` module, same Anthropic tool-use pattern as `assessTestCaseRisk`) plus `testPlans.generateStrategyDraft`, which grounds the prompt in a REAL summary of the project's existing coverage (test-type counts, detected `FrameworkFamily`s from `TestCaseSource`) rather than a generic template. The draft is never auto-saved - it's returned for review, and only becomes a real `TestPlan` when the user explicitly clicks "Create" on the review panel (same review-before-commit shape as an AI-reverse-engineered test case). New "Generate strategy with AI" modal on the test-plans page: prompt in, four editable lists out, then create. User-triggered per click, not a recurring/automated cost, so no rate limiting was added (matches `assessTestCaseRisk`'s precedent). Hit and fixed a real live-model issue during verification: the model's tool-use response leaked an internal XML-style call representation (`<parameter name="x">[...]</parameter>`) into each array field as a literal string instead of a real JSON array; added defensive parsing (`coerceStringArray`) that recovers the actual array data from that shape rather than failing generation outright. Verified against live Kall data with a real Anthropic API call: the generated risk areas correctly cited Kall's actual test counts by type (e.g. "current 32 E2E and 21 functional tests target the prior flow"), all four lists were populated, and the draft round-tripped correctly through a real `TestPlan` create.
- ✅ **P4-09** (M) Ground QA strategy generation in a real build/release (PRs in a release, or a build) instead of only manual prompt entry. Direct user request (2026-08-28). `labels: area:ai, area:api, type:feature` — new `changeImpact.ts`'s `getCommitLog(repoUrl, baseRef, headRef)`: a plain `git log --no-merges` between two refs, deliberately chosen over hitting a provider-specific PR API - for the common GitHub-flow case (squash-merge on PR merge) each commit subject already IS the PR title, and this works identically for any git host with zero installation/API-token dependency (unlike `P6-01`'s PR scanning, which does need a real GitHub App installation). Capped at 200 commits. `testPlans.generateStrategyDraft` gets optional `baseRef`/`headRef` - when given, the real commit log is fetched and fed to the model as authoritative "what's actually shipping" context alongside the free-text prompt, with an explicit instruction to flag it if the prompt and the real commits seem to disagree rather than silently picking one. The exact commits used are returned (`groundedInCommits`) and shown in the review UI, not just trusted silently. New "Ground in a real build" checkbox + base/head ref inputs on the strategy-generation modal, shown only when the project has a repo connected. Deliberately NOT attempted: parsing an actual Jira release page - would need real Jira API credentials this environment doesn't have; flagged as a real gap, not silently skipped. Verified for real against Vaettir's own actual GitHub repo and real commit history (11 real commits spanning that day's own feature work) with a genuine Anthropic call: every risk area correctly cited the *actual* shipped features by name (requirements extraction, shared step libraries, exploratory testing, etc.) rather than generic boilerplate, and the model correctly caught and flagged that the generic "Q3 release" prompt didn't match what the real commits actually showed - proving the "note the tension" instruction works, not just the grounding itself. Confirmed an empty commit range is rejected with a clear error, and confirmed the AI credit charge stays flat (10) regardless of whether grounding is used.
- ✅ **P4-03** (M) Risk-area inference: cross-reference `RiskFlag`s, historical `TestResult` failure rates, and compliance control gaps to suggest risk areas automatically rather than requiring the user to type them from scratch. `labels: area:ai, area:api, type:feature` — shipped as `testPlans.suggestRiskAreas`, deliberately rule-based rather than another LLM call (P4-02 already covers prompt-driven generation) - each of the three signals is a real, checkable fact: open `RiskFlag`s on the project's releases (most severe first), test cases with a real recent failure rate (>=30% over at least 3 of their last 20 results, capped per-case at that 20-result lookback), and compliance controls with zero mapped test cases across every framework the project actually maps controls under. Surfaced as a "Suggest from existing data" button under the QA strategy form's Risk areas field (P4-01), appending only genuinely new suggestions rather than duplicating ones already listed. Verified against live Kall data: confirmed an empty baseline with no signals present, then seeded one real open CRITICAL risk flag, a real test case put through a 3-fail/1-pass ingestion sequence via the actual `ingestJUnit` pipeline, and a real compliance control with zero mappings (while another control was mapped, to prove the "gap" list isn't just every unmapped control everywhere but specifically ones under a framework the project is actually using) - all three signal types surfaced correctly with accurate rationale text, then cleaned up.
- ✅ **P4-04** (M) Strategy-to-plan linking: a QA strategy should be able to spawn/reference concrete `TestPlan`s (e.g. "regression plan for payments") so the strategy isn't just a document but an actual coordination hub. `labels: area:api, area:db, type:feature` — shipped as a self-relation on `TestPlan` (`strategyId`/`strategy`/`linkedPlans`, a simple one-parent link rather than a join table), a `setStrategyLink` mutation (link set from the concrete plan's own side, target validated as same-project and `QUALITY_STRATEGY`-category, self-link rejected), and a `strategiesInProject` query feeding the picker. UI: a QUALITY_STRATEGY plan's detail page shows every plan currently pointing back at it ("Plans supporting this strategy"); any other plan gets a "Supports strategy" picker/unlink control. Verified against live Kall data: created a real strategy plan and a real regression plan, confirmed the strategy appeared in the picker's candidate list, confirmed self-linking and linking to a non-strategy plan were both correctly rejected, then linked, confirmed both sides of the relation reflected it correctly, unlinked, and cleaned up.
- ✅ **P4-05** (S) Strategy plan versioning/history (strategies evolve release to release; need to see what changed). `labels: area:db, area:api, type:feature` — shipped as a new `TestPlanVersion` model, a full snapshot (name, description, status, customFields) taken on every `create`/`update` via a `snapshotTestPlanVersion` helper, sequentially numbered per plan. Deliberately a full snapshot rather than reusing the `AuditLog` (P3-06): the audit log's one-line summary answers "an update happened," not "what did the risk areas actually say two releases ago." Not scoped to just `qa-strategy` - every plan type gets history, since "strategies evolve release to release" is really "any plan a team keeps coming back to evolves." A new `history` query returns every version, and a "History" section on the test plan detail page lists each version with a diff computed client-side against the prior snapshot (name/description/status changes plus which `customFields` keys changed). Verified against live Kall data: created a plan and updated it twice, confirmed exactly 3 sequential versions were recorded with the correct status/name/customFields at each point.
- ✅ **P4-06** (M) Exit-criteria tracking dashboard: live status of each strategy's exit criteria against current test/coverage data. `labels: area:web, type:feature` - *(this is a preview of the Phase 7 dashboard, scoped to one strategy)* — deliberately does NOT try to auto-grade each free-text exit criterion MET/NOT_MET - "Zero open Sev1 risk flags" and "95% of the E2E suite passing" are prose, and parsing arbitrary prose into a checkable predicate is exactly the problem `P7-02` (`AcceptanceCriterion` status) already punts on even with this same pipeline available now. Instead shipped `testPlans.strategySignals` plus a "Current signals" panel on the QA strategy plan's page: recent pass rate (last 200 results project-wide), latest coverage %, open risk flags by severity, and current flaky-test count - the real facts a human needs to eyeball their own exit criteria against, genuinely computed from `P5`'s now-real data pipeline rather than a guess. Caught and fixed a real bug during verification: the pass-rate query filtered on `testResult.testCase.projectId`, which silently excluded every *unmatched* result (no linked `TestCase` yet - the common case right after ingestion, before `P5-04`/`P5-14` catch up) instead of counting it; fixed to filter on `testResult.testRun.projectId`. Verified against live Kall data: confirmed an all-zero/null baseline, then seeded a real 3-pass/1-fail sequence via `ingestJUnit`, a real coverage report, and a real open risk flag, and confirmed all four signals - including the just-fixed pass rate - came back exactly correct.

---

## Phase 5 - Execution Ingestion & Framework Evaluators

Blending real test results from many frameworks/CI systems into the
platform, and moving known frameworks off pure-LLM parsing onto real
parsers for speed/cost/determinism.

### Epic 5.1 - Result ingestion
- ✅ **P5-01** (M) Generic JUnit XML ingestion endpoint (the de facto universal format - Jest, pytest, Go, Cypress, Playwright, and most CI systems can all emit or convert to it). `labels: area:api, type:feature` — shipped as `testRuns.ingestJUnit`, callable with a CI service-token API key exactly like any other mutation (no separate auth path needed - an ApiKey-backed User is a real Membership). A new `junitParse.ts` (via `fast-xml-parser`) normalizes the three real-world root shapes (`<testsuites>`, a bare `<testsuite>`, and the single-testcase case where the array wrapper collapses) into a flat list of PASS/FAIL/SKIP results with duration and failure message. Matching against existing test cases is intentionally scoped to an exact `TestCaseSource.externalTestId` lookup only - the deterministic case the schema already models for this; fuzzy classname/file-path matching for a test with no `externalTestId` yet, and surfacing unmatched results for manual linking, is P5-04's job, not this ticket's. New read-only "Test Runs" page lists ingested runs and drills into each result. This is the actual data pipeline several other roadmap items were blocked on (P4-03, P4-06, P3-05, P3-07, P7-02 all need real `TestResult` rows to exist) - none of those are unblocked by this ticket alone (still need P5-04's matching and/or a UI to act on the data), but the rows can now exist for real. Verified against live Kall data: ingested a real 3-case JUnit payload (one exact-externalTestId match, one failure with a captured error message, one skip), confirmed the created `TestRun`'s aggregate status, each result's status/duration/error, and the exact-match case correctly resolved to its real `TestCase` title while the other two correctly came back unmatched; also unit-verified the parser against a single-suite/single-case XML, an `<error>` (vs `<failure>`) element, and a testcase with no `classname` attribute.
- ✅ **P5-02** (S) GitHub Actions integration: a reusable workflow step/action that posts `TestRun`/`TestResult`s to the API at the end of a CI run. `labels: integration:github, type:feature` — shipped as a composite action at `.github/actions/report-test-results` (`action.yml` + `report.sh` + a usage README). Reads a JUnit XML file, derives `commitSha`/`branch`/`ciRunUrl` from the standard GitHub Actions context vars, and POSTs straight to `testRuns.ingestJUnit` (P5-01) with a plain `curl` call authenticated by an org API key - no client library or extra dependency needed beyond `curl` and the `node` every GitHub-hosted runner already ships, which builds the JSON body (so a failure message's quotes/newlines/non-ASCII content never has to survive shell string interpolation). Verified for real, not just read through: ran `report.sh` directly with the exact env vars a GitHub Actions runner sets, pointed at the local dev server with a real (temporary) API key, and confirmed the created `TestRun` had the correct derived `commitSha`/`branch`/`ciRunUrl` and both results' PASS/FAIL/error data; also verified the failure path - a bad API key correctly produces a non-zero exit code from the script, so a CI job actually fails loudly on a broken integration instead of silently swallowing it.
- ✅ **P5-03** (M) CircleCI + Jenkins ingestion adapters (translate their native result formats/webhooks to the same `TestRun` shape). `labels: area:api, type:feature` — CircleCI (`store_test_results`) and Jenkins (the JUnit plugin) both already standardize on JUnit XML for test reporting, so there was no separate native format to translate - the actual gap was deriving each platform's CI context from its own standard env vars (`CIRCLE_SHA1`/`CIRCLE_BRANCH`/`CIRCLE_BUILD_URL` for CircleCI, `GIT_COMMIT`/`GIT_BRANCH`/`BUILD_URL` for Jenkins) and posting to the same `testRuns.ingestJUnit` (P5-01) that P5-02's GitHub Action already proved out. Shipped as two standalone scripts under `ci-integrations/{circleci,jenkins}/report.sh` (plus a top-level README) rather than GitHub-specific composite actions, since neither platform has an equivalent "local composite action" mechanism - a plain script dropped into the pipeline config is the actual native pattern for both. Verified for real: ran both scripts directly with each platform's exact standard env var names set, pointed at the local dev server with a real (temporary) API key, and confirmed both created `TestRun`s had the correct provider/commit/branch/run-URL derived from that platform's own vars; also confirmed the required-env-var guard exits non-zero with a clear message when a var is missing.
- ✅ **P5-04** (M) Test result ↔ `TestCase` matching: map an incoming `externalTestId` (e.g. `"CartTest::test_empty_checkout_throws"`) to an existing `TestCase.source` record via `TestCaseSource.externalTestId`; surface unmatched results for manual linking when auto-onboarding (`P5-14`) is off or low-confidence. `labels: area:api, type:feature` — the exact-match half was already built alongside `P5-01`; this ticket adds the manual half: `testRuns.linkResultToTestCase` plus a "Link to test case" picker on every unmatched result in the Test Runs detail page. The link is remembered, not one-off - if the target `TestCase`'s source has no `externalTestId` yet, linking sets it, so the *next* run of that same test auto-matches without anyone linking it again. Refuses to silently overwrite a source already mapped to a *different* external id, since that would quietly break whatever result stream already resolves through it. Verified against live Kall data: ingested an unmatched result, linked it to a real test case, confirmed the result's `testCaseId`/title updated and the source's `externalTestId` got set, re-ingested the same external id and confirmed it now auto-matched, then confirmed a second, different result was correctly rejected when it tried to link to the now-already-mapped test case.
- ✅ **P5-05** (S) Flaky test detection: flag a `TestCase` as flaky when its results alternate PASS/FAIL across runs on the same commit/branch beyond a threshold. `labels: area:api, type:feature` — new `TestCase.isFlaky`/`flakyDetectedAt` fields, recomputed by `recomputeFlaky` (groups a case's last 10 results per-branch, flags flaky when a branch shows >=2 PASS/FAIL transitions) after every `ingestJUnit` call and every manual `linkResultToTestCase`. Deliberately NOT sticky - recomputed fresh each time rather than only ever flipped on, so a test that's stabilized clears its own flag instead of carrying a stale warning forever (a permanently-flaky-flagged test that got fixed is worse than useless, it's actively misleading). Surfaced as a 🎲 flaky badge next to the title on the test-cases list, matching the existing badge style used for review status. Verified against live Kall data: fed a real test case a PASS/FAIL/PASS/FAIL sequence through real `ingestJUnit` calls and confirmed `isFlaky` flipped true; fed a different case 3 consecutive PASSes and confirmed it correctly stayed false; confirmed the flag surfaces through `testCases.list`.
- ✅ **P5-06** (M) Coverage data ingestion (Istanbul/nyc, coverage.py, JaCoCo) - separate from pass/fail results, feeds the release-readiness coverage-gap logic in Phase 7. `labels: area:api, type:feature` — new `CoverageReport`/`CoverageFileEntry` models plus `coverage.ingest`, with real parsers (`coverageParse.ts`) for all three named formats: Istanbul/nyc's `coverage-final.json` (statement counts as the line-coverage proxy, since Istanbul doesn't track "lines" as a first-class unit), Cobertura XML (what `coverage.py`'s `coverage xml` actually emits, and convertible-to from many other ecosystems), and JaCoCo XML (a genuinely different shape - `<counter>` summaries per `<sourcefile>`, not per-line hit counts the way Cobertura/Istanbul are). `testRunId` is optional (a coverage step is often separate CI from whatever posted JUnit results) but validated against the given project when provided. Only ingestion + a per-file breakdown query ship here - the actual coverage-gap dashboard (which files are under a defined threshold) is Phase 7's job, this just makes the ingested data exist and visible on a new "Coverage" section on the Test Runs page. Verified all three parsers against realistic sample reports with hand-computed expected line/branch counts (all three matched exactly), then verified the full ingest mutation against live Kall data: a standalone Istanbul report, a Cobertura report linked to a real `TestRun`, and confirmed a coverage report claiming a `testRunId` from another project is correctly rejected.
- ✅ **P5-15** (M) **Failure evidence capture**: a thin reporter/plugin for the major front-end test runners (Playwright, Cypress, WebdriverIO) that captures a screenshot on every run and, for video, keeps a rolling trailing buffer - but only *uploads* either as a `TestResultArtifact` when the test actually fails. Passing runs discard the capture immediately, so this doesn't quietly balloon storage. Headless/no-display runs simply produce nothing, which is the expected case, not an error. Schema (`TestResultArtifact`, types `SCREENSHOT`/`VIDEO`) is already in place; this ticket is the runner-side capture + the ingestion endpoint's multipart upload path (built alongside `P5-01`). `labels: area:api, type:feature` — no blob storage existed anywhere in this codebase before this ticket, so real infrastructure had to be provisioned first (with explicit user approval): a new private `vaettir-test-artifacts-094842496450` S3 bucket (public access blocked, SSE-AES256, 90-day expiration lifecycle) and an inline IAM policy on `vaettir-api-task-role` scoping `PutObject`/`GetObject` to just that bucket. API side: `testRuns.requestArtifactUpload` (rejects any non-`FAIL` result outright) generates a presigned S3 PUT and creates the `TestResultArtifact` row immediately with the object's canonical (non-presigned) URL; `getArtifactViewUrl` re-signs a short-lived GET on demand - the bucket is never public, and the API server itself never touches the actual bytes either way. Runner side: **Playwright only** for now (Cypress/WebdriverIO need their own runner-specific plugin architecture, documented as follow-up in `ci-integrations/playwright/README.md`) - `vaettir-reporter.mjs` relays whatever Playwright's own `screenshot: 'only-on-failure'`/`video: 'retain-on-failure'` config already captured (no custom capture code needed) into a manifest + its own JUnit XML (built independently of any other JUnit reporter also configured, so the two files' test identifiers are guaranteed to agree with each other rather than depending on two separate tools deriving matching ones), and `report.mjs` posts the XML to `ingestJUnit` then uploads each failed test's files. Verified for real, twice over: (1) the S3 presigned upload/download round-trip directly (PUT real bytes, GET them back, byte-for-byte match), plus the FAIL-only rejection guard; (2) the entire runner pipeline against a real, live Playwright test project (one passing test, one intentionally failing with a real captured screenshot) - caught and fixed a real bug where `test.titlePath()`'s exact array shape produced a name with a leading blank segment that the manifest didn't trim but the server's JUnit parser did, silently breaking the match (fixed by filtering blanks instead of slicing a fixed count); after the fix, downloaded the actual uploaded screenshot back via a signed URL and confirmed it was a real, valid 1280x720 PNG linked to the correct (and only the correct) `TestResult`.
- ✅ **P1-19** (M) Attachments on a test case itself. Found via the 2026-08-27 competitor parity audit: every researched competitor supports attaching a reference file (mockup, log, spec doc) to the case's own authoring record - `P5-15` only covers evidence from a *failed execution*, a different thing. `labels: area:api, area:web, type:feature` — new `TestCaseAttachment` model (25MB cap - generous for a doc/log, not a video) and the same private-bucket, presigned-both-ways pattern `P5-15` already proved out (`artifactStorage.ts` widened with a generic content-type upload path rather than the fixed `SCREENSHOT`/`VIDEO` union). New `testCaseAttachments` router and an "Attachments" section on `TestCaseDetailContent` (upload, view via a fresh signed URL, delete). Verified for real against the live Kall project and the real S3 bucket (not a mock): a genuine file PUT through a real presigned URL, listed correctly, downloaded back through a real presigned GET with byte-exact content, the 25MB size cap correctly rejected an oversized request, and delete correctly cleared the list. **Found while verifying**: the same `TEST_ARTIFACTS_BUCKET` env var `P5-15` depends on was never actually added to the production `vaettir-api` task definition either (confirmed via `aws ecs describe-task-definition` - only `API_PORT` is set) - so both this feature and `P5-15`'s own failure-screenshot capture are non-functional in production today, purely a missing plain (non-secret) env var on the task definition. Flagged in `NEEDS_ATTENTION.md` rather than pushed as a task-definition change without asking.
- ✅ **P1-20** (M) Session-based exploratory testing. Found via the 2026-08-27 competitor parity audit: qTest and PractiTest both have this natively; Xray only offers it as a separate paid add-on; TestRail has a case template for it. Vaettir had nothing. `labels: area:api, area:web, type:feature` — new `ExploratorySession`/`ExploratorySessionNote` models, deliberately NOT built on `TestRun`/`TestResult` (`P5-16`'s manual execution) - an exploratory session has no predefined steps or pass/fail to record against, it's a charter plus a free-form timestamped log, a genuinely different shape of record. New `exploratorySessions` router (start/addNote/complete/list/byId) plus `convertToTestCase`, the "turn a session into a scripted test" move both qTest and PractiTest support - builds a real `TestCase` (`testType: EXPLORATORY`) from the charter (as `given`) and every note flagged as a **finding** (as `then`), deliberately excluding routine notes logged just to keep the session's narrative complete, so the resulting regression case reflects only what was actually worth flagging. New `/exploratory` (list + start) and `/exploratory/[sessionId]` (live note-taking, complete, convert) pages. Verified for real against the live local dev database: ran a full session (3 notes, 2 flagged as findings), confirmed list/detail counts were exactly right, completed it, converted it to a real test case, and confirmed the resulting case's `then` contained *only* the 2 findings - not the 1 routine note - proving the filter is real, not just "include everything." All synthetic data deleted after verification.
- ✅ **P1-21** (M) Data-driven / parameterized testing for the structured step-table format. Found via the 2026-08-27 competitor parity audit: Gherkin already gets this for free via Scenario Outline + Examples (`P2-13`), but the structured step-table format (`P1-10`'s other authoring option) had no equivalent, and qTest/PractiTest/Zephyr/Xray all support it for whichever authoring format they use. `labels: area:api, area:web, type:feature` — new `TestCaseDataset` model (one per case - parameters + named rows) and a new shared `@vaettir/core` helper `substituteDatasetPlaceholders`, using the exact same `<placeholder>` syntax as Gherkin's own Examples tables so the mental model matches across both formats (4 new unit tests). New `testCaseDatasets` router: `get`/`save`/`delete` plus `expandedPreview`, which returns each row's fully-substituted given/when/then - real substitution computed server-side, not a client-side approximation. New "Data set" section on `TestCaseDetailContent`: a parameter/row editor plus a collapsed-by-default preview per row. The dataset never mutates the case's own template steps - they stay the source of truth with placeholders intact. Verified for real against the live local dev database: created a case with `<role>`/`<action>`/`<expectedResult>` placeholders, saved a 2-row dataset (admin vs. viewer), confirmed each row's expanded preview substituted correctly and *differently* per row, confirmed the case's own stored `given` still showed the raw `<role>` placeholder untouched, and confirmed delete correctly cleared the dataset. All synthetic data deleted after verification.
- ✅ **P5-14** (L) **Continuous coverage listening**: when a `TestResult` arrives with no `TestCaseSource` match (`P5-04`), don't just queue it for manual linking - fetch that test's source via the repo connector (`P2-02`, using `TestResult.externalFilePath` + the run's commit sha) and auto-enqueue a scoped `ReverseEngineerJob` (`inputType: CI_UNMATCHED_RESULT`) against just that file/function. The resulting `TestCase` lands in the same AI-review queue as any other reverse-engineered case (`P2-06`), and on success its `TestCaseSource.externalTestId` is set so the *next* run of that test matches immediately instead of re-flagging. This is what turns reverse-engineering from something someone has to remember to run into a standing guarantee: every test CI actually executes ends up with a readable counterpart, without a human ever pasting anything in. Depends on `P2-02` (repo connector) and `P2-01`'s job worker. `labels: area:api, area:ai, type:feature` — both dependencies were already done, so this was genuinely unblocked. `junitParse.ts` now reads a `file` attribute when a JUnit reporter emits one (pytest, Vitest, and others do; never guessed/derived from `classname`, since a Java-style dotted-classname-to-path heuristic has real failure modes not worth risking). `continuousListening.ts`'s `autoEnqueueUnmatchedResult` fetches that file at the run's commit (new `fetchFileAtCommit` in `changeImpact.ts`, reusing the same clone/resolve-ref helpers the existing change-impact diff analysis already uses) and creates a `CI_UNMATCHED_RESULT` job with content already populated - matching the same content-fetched-at-enqueue-time pattern the existing repo-scan job creation already uses, so the worker itself needed no lazy-fetch logic. Called fire-and-forget from `ingestJUnit`, not awaited: a repo clone can take real time, and the CI job that's waiting on that HTTP response shouldn't get slower because of a background enrichment step. On job success, the worker links the produced `TestCase`'s source back to the triggering result's `externalTestId` only when the match is unambiguous - the produced case whose `sourceFunctionName` matches the reported test's name, or the sole case if the file only produced one - falling back to manual linking (`P5-04`) rather than guessing when a file produces multiple cases with no name match. Verified for real against the live Kall GitHub repo: ingested an unmatched result with a real file path and the repo's actual HEAD commit, confirmed a `CI_UNMATCHED_RESULT` job was auto-created and picked up by the background worker within seconds (no manual poke needed), confirmed it fetched the real file content and ran a real Anthropic reverse-engineering call that produced real `TestCase`s, and confirmed the linking step correctly declined to guess when the file produced multiple ambiguous candidates with no exact name match (the honest, expected outcome for that file's actual test style) - then separately verified the exact-match linking logic in isolation with a fabricated single-candidate scenario to confirm the positive path works correctly too.

- ✅ **P5-16** (L) **Native manual test execution.** Found via the 2026-08-27 competitor parity audit ([COMPETITIVE_ANALYSIS.md](COMPETITIVE_ANALYSIS.md)) as the single biggest structural gap: every researched competitor (TestRail, Zephyr, qTest, Xray, PractiTest) has a manual execution UI as a core feature, and Phase 5 was ingestion-only until now - nothing let a human BE the source of a real `TestResult`. `labels: area:api, area:web, type:feature` — deliberately reuses the existing `TestRun`/`TestResult` models (`ciProvider: "manual"`, `commitSha`/`branch: "manual"` sentinels) rather than a parallel model, since a manual run and a CI run are the same shape of fact just produced by a different actor - this is exactly why it needed zero changes anywhere else in the codebase to work with `P7-02`'s acceptance-criteria rollup, `P5-05`'s flaky detection, `P7-07`'s trend charts, or the existing Test Runs page. New `manualExecution` router: `start` (scope a run to chosen test cases, verified same-project), `getForExecution` (the execution screen's data source - both authoring formats per case, matching `P1-10`), `recordResult` (idempotent - re-recording a case updates in place rather than duplicating, rejects a case outside the run's planned scope, triggers the same flaky-recompute/healing-resolution signals a CI result already does), and `complete` (aggregate status via the same rollup rule `ingestJUnit` uses, plus a new `BLOCKED` status forcing `FAILED`). New `TestResultStatus.BLOCKED` value and `TestResult.note` field (a free-text "what actually happened," distinct from `errorMessage`). New "Run manually" bulk action on the Test Cases page and a step-through execution screen at `/test-runs/manual/[testRunId]`; the Test Runs list gets a "Resume" link for any still-`RUNNING` manual run. Scoped to case-level results (one status + note per case per run, matching the granularity every other part of this schema already uses) - step-by-step pass/fail within a single case is a real, separate increment, not attempted here. Verified end-to-end for real against the live Kall project: started a run against 4 real test cases, confirmed all four correctly showed no result initially, recorded a real PASS/FAIL/BLOCKED mix, confirmed re-recording a case updates its existing row rather than creating a duplicate, confirmed a case outside the run's scope is correctly rejected, confirmed `complete()` correctly computed `FAILED` from the still-open `BLOCKED` case, and confirmed the manual run is fully visible through the *existing*, unmodified `testRuns.list`/`testRuns.byId` queries with zero special-casing needed. All synthetic run/result rows deleted after verification.

### Epic 5.2 - Native framework evaluators
- ✅ **P5-07** (L) Native JS/TS parser (via `@babel/parser` or `ts-morph`) for Jest/Vitest/Mocha structure extraction - pulls `describe`/`it` blocks, assertions, and test names deterministically, then hands only the extracted structure to the AI agent for BDD phrasing (cheaper, faster, more reliable than raw-source-to-agent for well-known frameworks). `labels: area:ai, type:feature, perf` — shipped together with `P5-11` (the registry it plugs into). `packages/ai-agent/src/evaluators/jsTsEvaluator.ts` parses with `@babel/parser` (`errorRecovery: true`, returns `null` rather than throwing on unparseable input), walks nested `describe`/`context`/`suite` blocks to build a full title chain per test (not just the leaf name), and for each `it`/`test`/`specify` slices the exact original source of its body plus any `expect(...)`/`assert...(...)` calls found within it - real source text, not regenerated code. Registered for `JEST`/`VITEST`/`MOCHA` only; still one LLM call per file (not one per test block), just with a pre-parsed per-test breakdown as the prompt instead of raw source - keeps the existing cost profile while giving the model structure it doesn't have to derive itself. A framework with no registered evaluator (everything except those three today) is completely unaffected - same raw-source prompt as before this ticket. Verified: unit-tested the extractor against nested describes, a bare `test()` outside any describe, Vitest's `it.skip`, Mocha's `function(){}` callback + chai-style assertions, a non-test file (correctly returns `null`), and malformed syntax (correctly returns `null`, doesn't throw) - caught and fixed a real duplicate-assertion bug where `expect(x).toBe(y)` was also separately matching its own inner `expect(x)` call. Then ran the full `reverseEngineerTestFile` pipeline with a real Anthropic call against a real Vitest file: exactly 3 test cases produced (matching the 3 `it()` blocks) with correct BDD phrasing and preserved framework detection.
- ✅ **P5-08** (L) Native Python parser (`ast` module via a small Python microservice, or a JS-side heuristic) for pytest structure extraction. `labels: area:ai, type:feature` — took the JS-side heuristic option: adding a whole new deployment unit/runtime (a Python microservice) to this monorepo for one evaluator was a much bigger commitment than the value justified, and Python's indentation-based syntax makes block boundaries unambiguous to scan line-by-line without a real parser - a `def`/`class` line's body is exactly every following line indented further than it, until one that isn't. `pytestEvaluator.ts` walks the file once, recognizing `class Test*` bodies (extracting each `def test_*` method inside, correctly skipping non-test methods like `setup_method`) and module-level `def test_*` functions, pulling `assert` lines as the assertions list. Not a full AST (no multi-line `def` signature handling, decorators read but not evaluated, string contents not excluded from indentation scanning) - deliberately: returning `null` on anything that doesn't look like clean pytest structure is always safe, since the caller just falls back to raw-source-to-agent, never a wrong extraction. Verified: a realistic file mixing two module-level tests and two `Test*` classes (one with two real tests, one with one) extracted all 5 correctly with the right class-qualified titles, correctly excluded a non-test `setup_method`, and correctly returned `null` for both a non-test file and a class with no test methods. Then ran the full pipeline with a real Anthropic call against a real class-based pytest file: exactly 2 test cases produced with correct BDD phrasing, including correctly describing a `pytest.raises(ValueError)` assertion as "should raise a ValueError."
- ✅ **P5-09** (M) Native JUnit/TestNG (Java) annotation-based extraction. `labels: area:ai, type:feature` — one evaluator covers both frameworks, since they share the same `@Test` annotation convention. Java is brace-delimited rather than indentation-based (`P5-08`'s Python approach doesn't transfer), so `javaEvaluator.ts` finds each `@Test`-annotated method's body by locating its opening `{` and depth-counting braces forward to the matching `}` - same idea for extracting each individual assertion call's full text, since a naive "stop at the first semicolon" regex truncates mid-call for anything like `assertThrows(X.class, () -> { doThing(); })`, where the lambda body's own semicolon isn't the real end (a real bug caught in verification, not a theoretical one - fixed with the same depth-counting approach applied to parens). Also caught and fixed a second real bug: `@DisplayName` lookup used a fixed 300-character backward window, which reached past a short method entirely and picked up the *previous* test's `@DisplayName` when two tests sat close together - fixed by clamping the lookback to the end of the previous test's own body rather than a fixed distance. Naive by design in one respect shared with `P5-08`: brace/paren depth-counting doesn't exclude literal `{`/`}`/`(`/`)` inside strings or comments, so it can misfire on unusual bodies - returns `null` rather than a wrong extraction whenever a method's braces don't cleanly resolve. Verified: a realistic JUnit 5 file (one `@DisplayName`'d test, one plain-named test with a lambda-wrapped `assertThrows`, one non-`@Test` helper) extracted exactly the 2 real tests with correct, non-colliding titles and the full untruncated nested assertion; a TestNG file (same `@Test` annotation, different import) also extracted correctly; a non-test file correctly returned `null`. Then ran the full pipeline with a real Anthropic call against the same JUnit file: exactly 2 test cases produced with correct BDD phrasing, including correctly describing the exception-throwing assertion. This completes Epic 5.2 (native framework evaluators) - `P5-07` through `P5-11` are all shipped.
- ✅ **P5-10** (M) Cypress/Playwright structural extraction (these are JS, so likely shares P5-07's parser with framework-specific step detection for `cy.*`/`page.*` calls). `labels: area:ai, type:feature` — registered against the same `jsTsEvaluator.ts` from `P5-07`, no separate parser. `cy.*`/`page.*` calls aren't pulled into a distinct step list (Cypress especially mixes actions and `.should(...)` assertions in one chain, and Playwright already uses `expect(...)` the same as Jest/Vitest) - they're already carried in `bodySnippet`'s full test-body source, which is what the agent reads. Caught and fixed a real, would-have-shipped-broken bug while wiring this up: Playwright's extremely common `test.describe(...)` namespaced form was being misclassified by the old single-name callee resolution (`test.describe` resolved to just "test", so an entire describe block was extracted as ONE leaf test case instead of being recursed into) - fixed by classifying against every identifier in the callee chain (filtering known modifiers like `.only`/`.skip`/`.each`) and checking describe-names before test-names, which also correctly resolves `describe.only(...)`/`it.skip(...)`-style modifier chains for every previously-registered family. Verified: unit-tested nested `test.describe(...)` (including a describe nested inside another describe) and `test.skip(...)` all classify correctly with correct title chains; confirmed the Jest/Vitest/Mocha unit tests from `P5-07` still pass unchanged after the callee-resolution rewrite; confirmed a real Cypress `cy.*`/`.should()` chain extracts correctly. Then ran the full pipeline with a real Anthropic call against a real Playwright file using `test.describe(...)` (the exact pattern that was broken before the fix): exactly 2 test cases produced with correct BDD phrasing, matching the 2 real `test()` blocks.
- ✅ **P5-11** (S) Framework evaluator registry/interface so adding a new native evaluator is a plugin, not a router change - formalizes the fallback-to-AI path that already exists conceptually in `@vaettir/core`'s `detectFramework`. `labels: area:core, type:architecture` — shipped together with `P5-07` (its first real plugin, proving the interface rather than shipping an empty registry). New `frameworkEvaluators.ts` in `@vaettir/core`: `registerFrameworkEvaluator`/`getFrameworkEvaluator` keyed by `FrameworkFamily`, an `extract()` contract that returns `null` (not a thrown error) rather than failing the whole reverse-engineer job when a file doesn't fit that evaluator's assumptions. `reverseEngineerTestFile` now looks up an evaluator for the detected family and only changes its prompt when one exists and successfully extracts something - P5-08/09/10 each register one more evaluator, not another branch in that function. Verified: confirmed exactly the three registered families (`JEST`/`VITEST`/`MOCHA`) report as registered and every other family (checked `PYTEST`/`JUNIT`) correctly reports unregistered, falling through to the unchanged raw-source path.

### Epic 5.3 - Custom framework evaluation
- ✅ **P5-12** (M) "Teach the platform your framework" flow: user provides 2-3 example test files from their custom/internal framework; the agent infers the structural pattern (how are test names decided? assertions? setup/teardown?) and the platform stores that as a reusable per-project heuristic instead of re-inferring from scratch every file. `labels: area:ai, type:feature` — new `CustomFrameworkHeuristic` model plus `inferCustomFrameworkHeuristic`/`saveCustomFrameworkHeuristic` (two-step, review-before-commit, same shape as `P4-02`'s strategy draft - the user can edit the inferred description before it starts shaping anything) and `listCustomFrameworkHeuristics`/`deleteCustomFrameworkHeuristic`. The saved description is passed as a hint into `reverseEngineerTestFile` (new optional `customFrameworkHint` param) - wired into both the synchronous `reverseEngineerFile` path and the async job worker, so it applies consistently whether a file arrives via paste, repo scan, or zip upload. Only affects `CUSTOM`-family files - a framework with a native evaluator (`P5-07`-`P5-10`) already gets deterministic extraction, which doesn't need or use a learned-from-examples description. New "...or teach the platform a custom framework" section on the reverse-engineer page: paste/upload 2-3 example files, infer, review and edit, save; lists learned patterns per project with usage counts. Verified for real, no shortcuts: fed two example files in a fully invented bespoke test format (`SCENARIO`/`GIVEN_STATE`/`DO`/`EXPECT_STATE` blocks - guaranteed to fall to `CUSTOM`, since it matches no `KNOWN_FRAMEWORKS` signature), confirmed a real Anthropic call correctly inferred the exact structural pattern, saved it, then reverse-engineered a completely unseen third file in the same style and confirmed the saved hint was actually used (`detectedFrameworkFamily: CUSTOM`, a correct single test case produced, and the heuristic's `usageCount` incremented from 0 to 1) - then cleaned up.
- ✅ **P5-13** (S) Custom framework confidence tracking + prompt to "graduate" a well-understood custom framework into a saved evaluator config. `labels: area:ai, type:feature` — shipped together with `P5-12`: `confidence` (the agent's own self-rating at inference time) and `usageCount` (incremented on every reverse-engineer call that actually leaned on the heuristic, tracked as part of `P5-12`'s wiring) were already part of the schema and the learned-patterns list. "Graduate" is a UI nudge, not code generation - past 10 uses, the pattern's list entry shows "well-understood - consider a native evaluator" rather than the platform attempting to auto-generate parser code for an interactively-taught pattern, which would be a real correctness risk with no way to verify the generated parser is actually right.

---

## Phase 6 - Smart PR Scanning

Given a diff, propose which test plans/cases should run and flag coverage
gaps. This is the feature that connects "what changed" to "what should be
tested," and feeds Phase 7's risk flags.

- ✅ **P6-01** (L) GitHub App: install on a repo, receive `pull_request` webhooks (opened/synchronize), verify webhook signatures. `labels: integration:github, type:feature, area:api` — real GitHub App "Vaettir PR Scanner" (App ID 4734796) registered with Contents:Read, Pull requests:Read&write, Metadata:Read permissions, subscribed to `pull_request`. New `services/githubApp.ts` (hand-rolled RS256 App JWT signing + installation-token exchange, no JWT library needed for one fixed-shape token) and `services/githubWebhook.ts` (`handlePullRequestWebhook`), wired into `server.ts` as `POST /webhooks/github` (and its `/api`-prefixed mirror) in its own Fastify encapsulation context with a raw-body content-type parser scoped to just that route, so HMAC verification (`verifyWebhookSignature`, `timingSafeEqual`) always sees GitHub's exact bytes without affecting tRPC's JSON parsing elsewhere. A live PR isn't tied to any Vaettir `Release`, so unlike the human-triggered `recommendForChange` flow this always persists a `TestSelectionRun` (no `RiskFlag`s - those still only make sense against a specific release being prepared) and reuses the same file-matching logic via a newly extracted `services/changeMatch.ts` (shared by both call sites so a PR scan and a manual pre-release check can't quietly drift apart). Verified for real: the JWT/App-auth chain against the live GitHub API (`GET /app/installations` returned 200, confirming crypto correctness before installation), then a locally-run dev server driven with genuinely HMAC-signed simulated payloads (GitHub can't deliver webhooks to localhost) covering: a bad signature (401), a non-`pull_request` event (skipped), a PR into a branch not in the project's trigger list (skipped), and a valid `pull_request` payload diffed against two real commits from the live Kall repo - correctly cloned the repo, computed the real changed-file diff, matched against `TestCaseSource`, and persisted a `TestSelectionRun` (test rows deleted after verification).
- ✅ **P6-05** (M) PR comment bot: post the recommendation (relevant test plans, coverage gaps, suggested new test cases) as a GitHub PR comment. `labels: integration:github, type:feature` — built as part of `githubWebhook.ts`'s `handlePullRequestWebhook`: when the project's `PrScanPolicy.commentMode` is `COMMENT` (the default), runs `getDiffContent` + `recommendTestPlansForDiff` (the same AI recommendation `P6-04` built) and posts a formatted summary (must-run test cases, coverage gaps, relevant test plans, suggested new test cases) via `getInstallationAccessToken` + `postPrComment` (GitHub's REST API treats a PR as an "issue" for the comments endpoint). `SILENT_FLAG_ONLY` skips both the LLM call and the comment, keeping the persisted run as the only record. The comment-posting path is wrapped so a GitHub API failure or LLM error can't turn into a 500 that makes GitHub retry-storm the whole scan - confirmed via the local verification above, where no installed repo yet meant the token exchange legitimately failed and the handler still returned a clean `200` with `commentPosted: false` and a reason, rather than crashing. Posting an actual PR comment end-to-end still needs the App installed on a real repo, which hasn't happened yet since the App was only just registered this session.
- ✅ **P6-02** (M) Diff analysis: fetch changed files for a PR, map file paths to existing `TestCaseSource.filePath`s to find directly-affected test cases. `labels: area:api, type:feature` — already fully built as `riskAnalysis.recommendForChange` (found during a later roadmap audit, not newly built - this ticket was simply never marked done). Diffs `baseRef...headRef` via `getChangedFiles` and matches against `TestCaseSource.filePath`, returning a risk-sorted must-run list with each match's reason.
- ✅ **P6-03** (L) Coverage-gap detection: changed files/functions with no mapped `TestCaseSource` at all → create a `RiskFlag` (`source: PR_SCAN_COVERAGE_GAP`) with severity based on file criticality (e.g. payments/auth paths weighted higher - configurable per project). `labels: area:api, type:feature` — also already built in the same `recommendForChange` mutation (found during the same audit): changed files with no matching test case become `RiskFlag`s on a given release, deduped against already-open gap flags for that release so re-running against the same PR's later commits doesn't pile up duplicates. Severity is flat `HIGH` today, not yet weighted by file criticality (payments/auth vs. everything else) - that per-project weighting config is the one real gap left against this ticket's full description.
- ✅ **P6-04** (L) AI-assisted test plan recommendation: given the diff (not just file paths - actual changed code), ask the agent which existing test plans are relevant and whether new test cases are warranted, returned as a structured recommendation. `labels: area:ai, type:feature` — new `riskAnalysis.recommendTestPlansForDiff`, deliberately a separate mutation from `recommendForChange` (`P6-02`/`P6-03`) rather than folded into it: that one is free and deterministic (pure file-path matching), this one is a real LLM call reading actual patch content, and bundling them would make the cheap always-useful check pay the cost of the expensive judgment-based one on every call. New `getDiffContent` in `changeImpact.ts` (`git diff --unified=3`, capped at 40k characters with an explicit truncation marker rather than silently cutting off mid-hunk) feeds the actual patch - not just changed file names - to the agent alongside the project's existing test plans, which returns which plans are relevant (grounded in what the diff actually changed, not just which files it touched) and whether the change looks like it introduces a coverage gap beyond what already exists. Short-circuits with no LLM call at all when the diff is empty. New "Also check with AI" button on the Test Strategy page alongside the existing deterministic analysis. Verified against two real commits from the live Kall GitHub repo with a genuine code change (new name-splitting logic in the extension's field matcher) and a real Anthropic call: correctly identified a real "autofill matcher" test plan as relevant, correctly excluded an unrelated "billing and invoicing" test plan, gave a rationale that named the actual functions/behavior the diff changed, and produced four specific, non-generic new-test-case suggestions (multi-word last names, conflict resolution between whole/split name matches, missing source data) that were genuinely about what that diff introduced rather than a restatement of testing-in-general; also confirmed the no-diff case correctly short-circuits to an empty result with no LLM call.
- ✅ **P6-06** (S) Configurable PR scan policy per project (which branches trigger scans, risk-weighting rules, comment vs. silent-flag-only mode). `labels: area:web, type:feature` — new `PrScanPolicy` model (one per project, created lazily - a project that's never configured this gets documented defaults, not a required setup step) plus `getPrScanPolicy`/`savePrScanPolicy`. Directly closes the real gap flagged when `P6-02`/`P6-03` were found already-built: coverage-gap severity was flat `HIGH`; now `recommendForChange` weights it via the project's `pathSeverityRules` (glob patterns via `minimatch`, first match wins, so a narrow override can sit ahead of a broad catch-all) and falls back to the same flat `HIGH` when a project has no policy or nothing matches - a project that's never touched this gets identical behavior to before this ticket. `triggerBranches` and `commentMode` are genuinely forward-looking (real branch-triggered scanning and PR commenting both need `P6-01`'s webhook, not built yet) but needed somewhere to live regardless. New "PR scan policy" section on the Test Strategy page. Verified against a real diff from the live Kall GitHub repo: confirmed the lazy default (`main`/`COMMENT`/no rules) before any policy exists, saved a real policy with a `CRITICAL` rule for the extension's source path, re-ran the existing `recommendForChange` analysis and confirmed the resulting coverage-gap `RiskFlag` picked up the configured severity exactly while a rule-less path would have fallen back to the default, and confirmed the existing re-run dedup logic (no duplicate flags on repeated runs against the same release) still holds unchanged.
- 🟡 **P6-07** (M) GitLab equivalent (merge request webhooks) - once the GitHub path is proven, this should mostly be adapter work if P6-01 through P6-05 are built with the provider abstracted. `labels: integration:gitlab, type:feature` — confirmed the prediction was right: `getChangedFiles`/`getDiffContent`/`matchChangedFilesToTestCases` (`changeImpact.ts`/`changeMatch.ts`) are already plain `git clone` + `git diff` against a `repoUrl`, never touching a GitHub-specific API - so the only genuinely new code is parsing GitLab's payload shape and (best-effort) posting the result back. New `services/gitlabWebhook.ts`'s `handleMergeRequestWebhook` mirrors `githubWebhook.ts`'s `handlePullRequestWebhook` structure exactly (same "always persist a `TestSelectionRun`, only spend an LLM call + post a comment when the project's `PrScanPolicy` asks for it" shape), wired as `POST /webhooks/gitlab` (+ `/api`-prefixed mirror). Auth is simpler than GitHub's HMAC-over-raw-bytes: GitLab project webhooks send one static shared-secret token in a plain `X-Gitlab-Token` header, checked with a `timingSafeEqual` compare (`gitlabApi.ts`'s `verifyGitlabToken`) against a new `GITLAB_WEBHOOK_SECRET` env var - no raw-body content-type parser needed, unlike the GitHub route. New `services/gitlabApi.ts`'s `postMergeRequestNote` (GitLab's `POST /api/v4/projects/:id/merge_requests/:iid/notes`, `PRIVATE-TOKEN` header) is real and correct but genuinely unverified end-to-end against a live GitLab instance - posting requires a real personal/project access token (`GITLAB_ACCESS_TOKEN`), which doesn't exist in this environment, same category of real-external-credential gap as the still-open GitHub App secrets (`P10-02`). Verified everything that doesn't need that token, for real, no shortcuts, mirroring `P6-01`'s own verification approach (a live dev server driven with simulated payloads, since neither GitHub nor GitLab can deliver webhooks to localhost): a wrong `X-Gitlab-Token` correctly 401s, a non-`merge_request` `object_kind` is correctly skipped, a target branch not in the project's trigger list is correctly skipped, and a genuine `merge_request` payload carrying two real commit SHAs from Vaettir's own GitHub history correctly cloned the repo, computed the real changed-file diff, matched a seeded test case by its real source file, and persisted a `TestSelectionRun` with the correct match reason - then correctly reported `notePosted: false` with the honest reason (`missing GITLAB_ACCESS_TOKEN`) rather than silently swallowing the gap. All test rows deleted after verification.

---

## Phase 6.5 - Test Self-Healing (Suggest-Only)

Vaettir doesn't execute tests or own test source - it ingests results from
whatever CI/test runner the project already uses (Phase 5). That rules out
the classic "self-healing" pattern (Mabl/Testim-style: the tool owns
execution, so it can auto-retry a healed locator and confirm the fix
before anyone sees a failure). What Vaettir *can* do: when a test fails,
use the AI agent to tell "this looks like a brittle/selector-style
failure, not a real regression" apart from a genuine behavior change, and
propose a concrete fix - surfaced for human review, same as every other
AI-generated content in this app (P2-06's review queue, P4-02's strategy
draft, P5-12's inferred heuristic). **Never edits the connected repo or
opens a PR on its own** - the repo connector (P2-02) is read-only today,
and giving an agent unsupervised write access to someone's actual test
source is a different, much higher-risk feature than anything else built
so far; if real autonomy here is wanted later, that's its own explicit
decision, not a default this phase reaches for.

Depends on real ingested failure data (Phase 5, done) and repo diff
access (`changeImpact.ts`'s `fetchFileAtCommit`/`getChangedFiles`, already
built for P5-14 - full PR-triggered diff analysis from Phase 6 isn't a
hard prerequisite, since a failure's own commit context is enough to
diff against).

- ✅ **P6.5-01** (M) Failure classification: given a failing `TestResult` with a matched `TestCaseSource`, diff the test's source (and the application code paths it exercises, where determinable) between the last commit it passed on and the failing commit; ask the agent to classify the failure as likely-brittle (selector/locator/text changed, structural refactor) vs. likely-real (behavior actually changed) with a rationale. Stored on the `TestResult`, not auto-acted on. `labels: area:ai, area:api, type:feature` — new `HealingSuggestion` model (one row per failing `TestResult`), `services/healingSuggestion.ts`'s `classifyAndSuggestHealing`, and `@vaettir/ai-agent`'s `classifyTestFailure` (real Claude call, `BRITTLE`/`REAL_REGRESSION`/`UNCERTAIN` with a rationale grounded in the actual source diff between the test's last-passing commit and the failing one, fetched via `changeImpact.ts`'s existing `fetchFileAtCommit`). Idempotent - re-classifying an already-classified result returns the existing row and spends no additional AI credits.
- ✅ **P6.5-02** (L) Healing suggestion generation: for a failure classified likely-brittle, generate a concrete proposed fix (e.g. an updated selector/locator, an updated expected value) as a diff-style suggestion against the actual test source - grounded in the real code diff, not a guess from the error message alone. `labels: area:ai, type:feature` — built into the same `classifyTestFailure` call as `P6.5-01` rather than a second round-trip: the agent only emits `suggestedDiff`/`suggestionRationale` when it's actually confident in a `BRITTLE` classification, explicitly instructed to prefer `UNCERTAIN` over guessing - a wrong suggestion is worse than none, since a human reviews this before touching anything.
- ✅ **P6.5-03** (M) Suggestion review UI: surface classification + suggested fix on the failing result (Test Runs detail page), with approve/reject/edit actions - approving marks it reviewed and copies the suggested diff somewhere the developer can actually apply it (clipboard/download), it does not touch the repo. `labels: area:web, type:feature` — "Classify failure" button + result panel under each `FAIL` row on the Test Runs page's run detail drawer, showing classification, rationale, and (when present) the suggested diff with Approve/Reject actions. No auto-apply anywhere - the diff is text on the page to copy, full stop.
- ✅ **P6.5-04** (S) Outcome tracking: when a test that had an approved healing suggestion later passes again (via normal `ingestJUnit` result flow), mark the suggestion as "resolved" - closes the loop on "did this suggestion actually help" without needing repo write access or a triggered re-run. `labels: area:api, type:feature` — wired into `testRuns.ingestJUnit` itself: any matched test case that comes back `PASS` in a new ingested run resolves its still-open `HealingSuggestion`(s) (reviewed or not - a pass closes the loop regardless of whether anyone got to review it first).
- ✅ **P6.5-05** (S) Aggregate signal: a project-level view of how many failures are brittle vs. real over time - if a specific test/file keeps generating brittle-failure suggestions, that's a signal the test itself (not the app) needs attention (e.g. a hardcoded selector that should be a data-testid). `labels: area:web, type:feature` — `healingSuggestions.aggregateSignal` + a section on the Test Runs page: total/brittle/real/uncertain/resolved counts and a "repeat brittle offenders" list (test cases with 2+ brittle classifications).

Verified end-to-end for real against the live Kall repo and a real Anthropic call: built a synthetic failing/passing pair of `TestResult`s against the real `apps/extension/src/matcher.js` file at two real commits, called `classify` - it fetched both real file versions via git, made a real LLM call, and returned a grounded, specific rationale (correctly reasoned through the actual code's name-splitting logic, not a generic response) classifying the synthetic failure as `REAL_REGRESSION`; confirmed re-classifying the same result returns the identical suggestion with zero additional AI credits spent; confirmed `approve` updates status; confirmed the aggregate signal query rolls up counts correctly; and confirmed posting a real passing result through the actual `ingestJUnit` path correctly resolved the open suggestion. All synthetic data deleted after verification.

---

## Phase 7 - Release Readiness & Quality Intelligence Dashboard

The rollup: CI/CD release-to-release visibility, coverage against defined
release acceptance criteria, and risk highlighting.

Split across two pages rather than one: **Test Strategy** (pre-execution -
risk, mitigations, coverage, run recommendations; what used to be
`risk-analysis`) leads into **Release Readiness** (post-execution - how
well a release adhered to that plan, actual results and readiness now).

### Epic 7.1 - Release model & acceptance tracking
- ✅ **P7-01** (M) Release management UI: create/manage `Release`s, attach `TestPlan`s (including a `release-readiness` typed plan) and acceptance criteria. `labels: area:web, type:feature`
- ✅ **P7-02** (M) Live acceptance-criteria status: each `AcceptanceCriterion` auto-computes `MET`/`NOT_MET`/`AT_RISK` from underlying test results rather than requiring manual status updates. `labels: area:api, type:feature` — new `services/acceptanceCriteria.ts`: a criterion's scope is every `TestCase` on its `TestPlan` (the same plan a QA lead builds to define "done" for a release); status rolls up from each test case's latest `TestResult` (by `TestRun.startedAt`) - any latest `FAIL` forces `NOT_MET`, any never-run/`FLAKY`/`SKIP` forces `AT_RISK`, all-latest-`PASS` gives `MET`. Computed live on every read (`releases.list`/`readiness`/`listTestPlans`), not persisted or cron'd - staleness would be most visible and most damaging to trust exactly on a release readiness page. A plan with zero test cases (e.g. a pure compliance sign-off criterion with no automated coverage) has nothing to compute from, so it falls back to the existing manually-set status - the manual `updateAcceptanceCriterion` mutation stays for that case. The release readiness page now shows a "(live)" read-only badge instead of the status dropdown whenever a criterion is computed, since a manual edit there would just be silently overwritten on the next load. Verified against four real scenarios built through the actual `testPlans`/`releases` routers (not raw DB inserts) with real `TestRun`/`TestResult` rows: a plan whose test case had an old `FAIL` followed by a newer `PASS` correctly resolved to `MET` (proving "latest by time" ordering, not "any failure ever"), a plan with a `FAIL`-only case resolved to `NOT_MET`, a plan with a case that had never run resolved to `AT_RISK`, and a plan with no test cases at all correctly kept its manually-set `MET` status untouched; the release-level `readiness` rollup (2 met / 1 at risk / 1 not met) matched the existing `P7-03` scoring formula exactly. All test rows deleted after verification.
- ✅ **P7-03** (S) Release readiness score: single rollup number/traffic-light per release from criteria status + open risk flags + compliance control coverage. `labels: area:api, type:feature` - explainable weighted formula (criteria completion minus severity-weighted open-flag penalty, any open CRITICAL forces BLOCKED); compliance control coverage isn't factored in yet.

### Epic 7.2 - Dashboard
- ✅ **P7-04** (L) Release readiness dashboard page: readiness score, acceptance criteria checklist, open risk flags (from PR scanning + coverage gaps + failing/flaky tests), trend vs. previous releases. `labels: area:web, type:feature` - trend vs. previous releases not built (see P7-07).
- ✅ **P7-05** (M) Granular drill-down: from the dashboard, click into any risk flag/failing test/uncovered file down to the actual `TestCase`/`TestCaseSource`. `labels: area:web, type:feature` - test plans/cases link through; individual risk-flag-to-source-line drill-down not built.
- ✅ **P7-06** (M) Holistic cross-project view: an org-level dashboard rolling up readiness across all projects/releases (useful once you have more than one team). `labels: area:web, type:feature` — new `releases.orgOverview` query (one row per project: its most recently active, not-yet-`SHIPPED` release and that release's readiness, reusing the exact same `computeReadiness`/`P7-02` live-status logic as the per-release page so the two can never disagree) plus a new top-level `/dashboard` page (added to the org sidebar) with a 4-up summary (ready/at-risk/blocked/no-active-release counts) above a per-project table. A project with nothing currently in flight shows `release: null` rather than being silently omitted. Verified the query logic for real against the live Kall org via the actual router - also caught and cleaned up two stray leftover `Release` rows from earlier verification work (one from a crashed script that skipped its own cleanup) that were polluting the real org's dashboard data. The page itself typechecks and reuses the already-shipped `ReadinessBadge` component and table conventions from the per-release readiness page, but hasn't been visually confirmed in a browser (no dev-login credentials available in this pass).
- ✅ **P7-07** (M) Release-to-release trend charts: pass rate, flaky test count, coverage %, mean time-to-green over the last N releases. `labels: area:web, type:feature` — `Release` has no direct FK to the `TestRun`s that ran during it, so new `releases.trend` defines a release's "window" as `(previous release's createdAt, this release's createdAt]` on the same project and buckets every `TestRun` that started in that span; computed over the project's full release history first (so the oldest *returned* release still gets a correct lower bound from the release before it) then trimmed to the last N. Mean time-to-green is the average gap between a FAILED→...→PASSED streak's start and its resolution within the window - a window with zero failures correctly reports `null`, not `0`, since there's nothing to measure. New dependency-free `components/TrendChart.tsx` (a handful of points per project doesn't justify a charting library) renders four small line charts (pass rate, coverage, flaky count, mean time-to-green) at the top of the project's Releases page. Verified against real synthetic data across two release windows on the Kall project: a FAIL-then-PASS-then-FLAKY sequence in one window correctly produced a 50% pass rate, 1 flaky result, a 1-day mean time-to-green, and the window's own coverage report's percentage; an all-passing second window correctly showed 100% pass rate and a `null` (not zero) time-to-green. All synthetic runs/releases deleted after verification.
- ✅ **P7-08** (S) Configurable release gates: block a release status from moving to `READY` while criteria are unmet (soft warning vs. hard block, per-org policy). `labels: area:api, type:feature` — new `Organization.releaseGatePolicy` (`SOFT_WARNING` default / `HARD_BLOCK`, admin-settable on the org settings page) and `services/releaseGate.ts`'s `evaluateReleaseGate`, reusing `P7-02`'s live-computed criterion status so the gate and the readiness dashboard can never disagree about what's actually met. "Unmet" is deliberately narrow - only a `NOT_MET` criterion or an open `CRITICAL` risk flag blocks (matching `computeReadiness`'s own `BLOCKED` trigger); `PENDING`/`AT_RISK` don't hard-stop since they're incompleteness, not failure. New `releases.checkGate` query the readiness page calls before attempting a `READY` transition, and real server-side enforcement in `releases.updateStatus` - only entering `READY` is gated, and only `HARD_BLOCK` actually throws (`SOFT_WARNING` still surfaces the gap via a browser confirm dialog but lets the user proceed), so it can't be bypassed by hitting the API directly under a strict org's policy. Verified against a real failing test case on the Kall project driven through the actual `testPlans`/`releases`/`organization` routers: `checkGate` correctly flagged the `NOT_MET` criterion, a `READY` attempt under `SOFT_WARNING` succeeded, the same attempt under `HARD_BLOCK` was correctly refused with the specific unmet-criterion reason in the error, and after fixing the underlying test (a new passing `TestResult`) both `checkGate` and the `READY` transition succeeded under `HARD_BLOCK` with no policy change needed - proving the gate re-evaluates live rather than caching a stale verdict. Org policy and all test rows restored/deleted after verification.
- ✅ **P7-09** (M) Slack/email digest: daily or pre-release readiness summary pushed to a channel. `labels: integration:slack, type:feature` — Slack half fully built; email deferred (see below). New `Organization.slackWebhookUrl`/`digestEnabled`/`digestHourUtc`/`lastDigestSentAt` (kept minimal and org-scoped rather than building Phase 9's general `P9-00` integration-config framework early), `services/readinessDigest.ts` (Slack Block Kit message built from the same `getOrgOverview` - extracted from `P7-06` into `services/orgReadiness.ts` - so the digest and the dashboard can never disagree), and `jobs/readinessDigestScheduler.ts`: a `reverseEngineerWorker.ts`-style in-process interval (no queue/cron infra exists) checking every 15 minutes for orgs whose `digestHourUtc` matches the current UTC hour and haven't sent yet today. `organization.sendTestDigest` covers both "test my webhook" and the ticket's "pre-release summary" on-demand case. The webhook URL is write-only from the client (`byId` only reports `slackWebhookConfigured: boolean`) since it's a bearer-token-like secret. New "Release readiness digest" section on the org settings page. Verified for real: posted an actual digest through the real send path to a local HTTP server standing in for a Slack incoming webhook (no real Slack workspace needed to prove the code's own HTTP behavior) - captured a correctly-formed Block Kit payload with accurate counts matching the live Kall org's real state, and confirmed `lastDigestSentAt` was updated; org settings restored after. Actually wiring this to a real Slack channel needs a real incoming-webhook URL from James - flagged in `NEEDS_ATTENTION.md`. Email delivery not built at all: there's no transactional email provider in this codebase yet (invitations are token-link based, no dispatch), and picking one (SES vs. Resend vs. Postmark) plus a verified sending domain is a setup decision, not something to guess overnight - flagged for later.
- ✅ **P7-10** (M) Interactive dashboard snapshot export: turn the release readiness dashboard into a shareable Confluence/Notion/Markdown/wiki snapshot that's interactable, not a static image. `labels: area:web, type:feature` — direct user request, explicitly distinct from the already-shipped live dashboard (`P7-04`-`P7-07`). New `releases.getSnapshot` query (`apps/api/src/routers/releases.ts`) returns one payload combining exactly what `readiness`/`listTestPlans`/`listRiskFlags`/`trend` already compute, so the export can never drift from the live page's own numbers - not a new formula. Two client-side generators in `apps/web/lib/snapshotExport.ts` turn that payload into a self-contained file, downloaded via a new shared `apps/web/lib/download.ts` Blob helper (also absorbed the release readiness page's existing CSV export, which had its own duplicate Blob logic): a `.html` export that's genuinely interactive with zero external dependencies (native `<details>` collapsible acceptance-criteria sections, a click-to-sort/filterable risk-flags table, inline SVG trend charts with real per-point hover tooltips, all via a small inline vanilla-JS `<script>`) so it renders the same as a standalone file, in Confluence's HTML macro, or in an iframe embed; and a `.md` export (GFM tables + `<details>` blocks) whose own footer honestly flags that Notion flattens `<details>` to static content on import (unlike GitHub/most wikis) - real interactivity there means using the HTML export instead. Both are a **download-and-paste/embed** workflow, not a live OAuth push into Confluence's or Notion's own hosted pages - that would be Phase 9 integration-framework territory needing per-customer API credentials that don't exist yet. Two export buttons added to the release readiness page. Verified against a real release on the Kall project: `getSnapshot`'s readiness/testPlans/riskFlags/trend cross-checked byte-for-byte against the live individual queries (all `OK`), the real payload was fed through both generators, and the generated HTML was loaded in a real browser and driven programmatically - the resolved-risk-flag filter correctly hid/showed the row, clicking the Severity column header correctly reversed sort order, and each SVG chart point's tooltip showed its own release's data (catching and fixing, before shipping, a bug in the original tooltip-index logic that could only ever show the first or last release's label regardless of which point was hovered).
- ✅ **P7-11** (M) AI-drafted release summary: a stakeholder-readable narrative (overview, what changed, coverage, risks, recommendation) generated from a release's real readiness data, for the audience the dashboard itself doesn't serve well - product/leadership who won't read acceptance-criteria tables. `labels: area:api, area:web, type:feature` — top pick from a speculative-planning pass James asked for after the safe backlog was exhausted (published as an artifact, "Where Vaettir Goes Next"), built directly on his follow-up instruction. New `packages/ai-agent/src/generateReleaseSummary.ts` (same tool-calling/`traceAnthropicCall` shape as every other ai-agent function) and `releases.generateSummaryDraft` mutation, which recomputes readiness/open-risk-flags via this same file's own `computeStatusesByTestPlan`/`computeReadiness` calls (never a second formula, so the AI summary can't mathematically disagree with the live dashboard) and optionally grounds `whatChanged` in a real commit log via `getCommitLog` (`P4-09`'s exact grounding pattern) when the caller supplies a `headRef`. The system prompt is deliberately strict about not softening a real `BLOCKED` state into false reassurance and about saying plainly when no commit log was given rather than guessing. Charges `AI_OPERATION_COSTS.generateReleaseSummary` (6 credits). Returns an unpersisted draft for review/edit - like every other AI-generation feature here, nothing is posted or saved automatically. New "Generate release summary" button and modal on the release readiness page (`GenerateSummaryModal`, mirroring the already-shipped `GenerateStrategyModal`'s grounding-checkbox/baseRef/headRef pattern), with a "Copy to clipboard" action for pasting into Slack/email/a doc. Verified end-to-end against real data: called through the actual `appRouter` caller (not a unit test) against the real Kall project's `v2.4 - Submission Pipeline & Security Hardening` release (a real BLOCKED/30-of-100 release with a real unmet-criteria gap and a real HIGH risk flag) - the draft correctly stated BLOCKED plainly with no hedging, correctly cited the specific HIGH risk flag by its real description, and correctly said build-level detail wasn't available when no `headRef` was passed; confirmed the grounding path resolves against the project's real connected repo. UI verified live in a browser against a throwaway seeded org/release (not Kall) since the local dev Clerk session didn't carry a real org membership: button opens the modal, generation round-trips through the real API, and all five fields render populated and editable. All throwaway seed data deleted after verification.

---

## Phase 8 - Mobile Parity

The mobile app is currently a read-only test-case list stub.

- ✅ **P8-01** (M) Auth in mobile (shares Phase 1's auth, native login flow). `labels: area:mobile, type:feature` — found already built during an overnight roadmap audit (never marked done): `apps/mobile/App.tsx` has a real Clerk Expo sign-in flow (`useSignIn`, `SignedIn`/`SignedOut`), and `lib/trpc.ts` wires the resulting session token into every tRPC call's `Authorization` header exactly like the web app does. Deliberately minimal UI and no sign-up flow - the code's own comment says as much ("enough to authenticate... a prettier UI and sign-up is Phase 8 work") - so this covers the ticket's literal scope (native login, shared Phase 1 auth) without claiming full mobile onboarding is done. Confirmed via code inspection and a clean `apps/mobile` typecheck, not a live device/emulator run tonight - no Android/iOS runtime available in this environment to click through it.
- ✅ **P8-02** (M) Test case detail + BDD view in mobile (parity with web's detail page). `labels: area:mobile, type:feature` — tapping a row in the existing list now opens a native `Modal` (no navigation library exists yet in `apps/mobile`, and per the roadmap's own framing the highest-value mobile use case is quick lookup, not authoring, so a modal rather than a full page route was the right scope) showing the full `testCases.byId` detail: title/type/priority/origin, both authoring formats (`given`/`when`/`then` BDD lines AND the structured step table with the project's real `stepFieldLabels`, same "supports either format" parity as web's `P1-10`), and tags. Read-only, matching `P8-01`'s precedent that a prettier/fuller mobile UI is later Phase 8 work. Confirmed via a clean `apps/mobile` typecheck and a full code read-through, not a live device/emulator run - same limitation `P8-01` already noted, no Android/iOS runtime available in this environment.
- ✅ **P8-03** (L) Release readiness dashboard, mobile-adapted view - the highest-value mobile use case is likely "check release status from your phone," not full authoring. `labels: area:mobile, type:feature` — a new "Releases" toggle next to "Test Cases" (no navigation library exists in `apps/mobile`, same modal/toggle pattern `P8-01`/`P8-02` already established) shows a plain, read-only list: readiness score/label (color-coded), status, acceptance-criteria breakdown, and open risk flag count (with a critical-count callout when relevant) per release. Reuses `releases.list` exactly as the web releases page's own summary does - same score/label/criteria/risk-flag computation, so mobile can never disagree with what the web dashboard shows for the same release. Deliberately read-only, matching the ticket's own framing and this session's `P8-01`/`P8-02`/`P8-06` precedent that a fuller/write-capable mobile UI is separate, later work - no status change, no criterion edit, no risk-flag resolve here. Verified via a clean `apps/mobile` typecheck and full code read-through; same live-device/emulator limitation already noted on every other `apps/mobile` ticket this session - nothing in this environment can run Expo. Not deployed anywhere (mobile ships via EAS/app stores, `P8-07`, which needs Apple/Google developer accounts this environment doesn't have) - this is source-only, on `master`, ready for a real device build.
- 🟡 **P8-04** (M) Push notifications for release-readiness state changes / compliance sign-off requests. `labels: area:mobile, type:feature` — scoped down to the compliance-sign-off half, which is a genuine end-to-end feature: no "sign-off request" concept existed before (`ComplianceSignOffRequest` was previously only a retroactive `ComplianceSignOff` record, created after the fact), so this adds a new model for the ask itself, independent of whether it's ever fulfilled. A Compliance Auditor/Admin/Owner can request a sign-off from another such member for a given control+period (`compliance.requestSignOff`); the target gets a real Expo push notification (new `pushNotify.ts` service, plain `POST` to Expo's push API, dead-token pruning on `DeviceNotRegistered`) and sees it surface both on web (new "Request a sign-off" section in the compliance evidence drawer) and mobile (a "Requested from you" banner atop `P8-05`'s compliance tab, tapping straight into `SignOffModal`). The moment that same signer actually signs off for the same control+period, `signOffControl` auto-fulfills the matching open request - no separate "mark done" step. Mobile registers its Expo push token on sign-in (`registerForPushNotifications` in `App.tsx`) via a new `user.registerPushToken` mutation, storing tokens in a new `PushToken` model. Verified for real: the full request → push → auto-fulfill → disappear-from-pending flow was run against real Kall data via a direct `appRouter` caller (created a request, confirmed it appeared in `myPendingSignOffRequests`, signed off, confirmed it was stamped fulfilled and dropped off the pending list); the Expo integration was proven against Expo's real production endpoint (sent a real-shaped fake token, confirmed a non-throwing response and correct dead-token pruning) rather than just typechecked; the new web UI was exercised live in the browser end-to-end (seeded a throwaway control, opened its evidence drawer, picked a member, entered a period, clicked Request, confirmed "Request sent." with no error - throwaway control and its request deleted afterward). Explicitly still open: real push token generation on a live device needs an actual EAS project (`apps/mobile/app.json` has no `extra.eas.projectId` - none has ever been created for this app, same category of gap as the GitLab access token and GitHub App secrets already logged in `NEEDS_ATTENTION.md`); and the "release-readiness state change" half of the ticket's original wording is deliberately deferred - release readiness is computed live from current data, never persisted as a state history, so notifying on a "change" would need new snapshot/diff infrastructure that doesn't exist yet and is out of scope for this pass.
- ✅ **P8-05** (M) Mobile approval flows: compliance sign-off and AI-reverse-engineered test case review, approvable from mobile. `labels: area:mobile, type:feature` — AI review-queue half done earlier this session: `P8-02`'s detail modal shows Approve/Reject (with an optional note) whenever a case's `reviewStatus` is genuinely `PENDING_REVIEW`, calling the same real `testCases.approve`/`.reject` mutations web uses. The compliance sign-off half - deliberately deferred earlier as needing its own scoped screen rather than a rushed modal add-on - is now built: a new "Compliance" tab in `App.tsx` (`ComplianceSignOffList`) lists real frameworks (`compliance.listFrameworks`) and, per framework, its real controls with mapped-test-case counts (`compliance.controlCoverage` - the exact same query the web compliance page uses, so mobile can never disagree about coverage); tapping a control opens `SignOffModal`, which shows its real sign-off history (`compliance.listSignOffs`) and a period + written-attestation-statement form that calls the exact same `compliance.signOffControl` mutation the web page does. The server's role gate (`COMPLIANCE_AUDITOR`/`ADMIN`/`OWNER`, checked explicitly since that role ranks below `EDITOR` in this app's general hierarchy) is the real enforcement regardless of what this screen shows - same pattern as every other mobile mutation. Verified for real, not just typechecked: a clean `apps/mobile` typecheck, then the exact call sequence this screen makes (`listFrameworks` → `controlCoverage` → `listSignOffs` → `signOffControl` → `listSignOffs` again) driven directly through the real `appRouter` caller against a throwaway org/project/control - confirmed a genuine sign-off persisted with the correct period/statement/signer and the follow-up `listSignOffs` call correctly showed it. Still not attempted: an actual live-device screenshot, same limitation noted throughout this session's other Phase 8 mobile work (no live device/emulator available in this environment).
- ✅ **P8-06** (S) Offline-friendly read caching for test case browsing (spotty connectivity shouldn't blank the app). `labels: area:mobile, type:feature` — new `@react-native-async-storage/async-storage` dependency; every successful `testCases.list` fetch is cached per-project, and a project switch paints the cache immediately (before the network round-trip resolves) rather than showing a blank list while loading. On a genuine fetch failure, falls back to the cache instead of replacing the list with an error screen - a small "Showing cached data" banner surfaces the degraded state instead of hiding it, and the error text only appears when there's no cache to fall back to either. Verified via a clean `apps/mobile` typecheck and code read-through, same live-device limitation as the rest of Phase 8's work this session.
- **P8-07** (M) App store submission prep (iOS/Android): icons, splash, privacy manifest, EAS build config. `labels: area:mobile, type:infra`

---

## Phase 9 - Integrations & Ecosystem

`P9-01`/`P9-02`/`P9-03` below are the basic (link/sync/notify) versions.
Queued per explicit request alongside Phase 7's dashboard work: these three
should eventually grow into **native, deeply-customizable surfaces** in
each tool, not just one-way notifications - a release readiness score,
acceptance criteria, and open risk flags should be *visible and
interactive* inside Slack/Jira/Linear themselves, not just linked back to
Vaettir. Not started; captured here so the basic versions below get built
with that end state in mind (e.g. don't hardcode a single fixed Slack
message format if a Block Kit App Home is the eventual destination).

- **P9-00** (L) Integration configuration framework: the shared per-org
  layer every tool-specific integration below builds on - which events
  route to which destination (channel/project/board), per-project
  overrides, field/status mapping, and a connection-health view (last
  successful sync, auth expiry). Building this before/alongside `P9-01`
  through `P9-03` avoids three independent, incompatible config systems.
  `labels: area:api, type:feature`
- **P9-01** (M→L) Jira integration: link `Requirement`s to Jira issues
  bidirectionally, sync status - then grow into a **Jira Forge app** that
  renders a live readiness/coverage panel directly on the linked issue
  (not just a link back to Vaettir), with customizable field mapping.
  `labels: integration:jira, type:feature`
- **P9-02** (M→L) Linear integration: same as above for teams using Linear
  for requirements (note: distinct from *using* Linear to track this
  platform's own build - this is the product feature) - then a Linear app
  with a custom panel/view showing live readiness on the linked issue.
  `labels: integration:linear, type:feature`
- 🟡 **P9-03** (S→L) Slack: starts as configurable event notifications (new
  risk flag, review-queue item, sign-off request) → channel, then grows
  into interactive Block Kit messages (approve/resolve a risk flag inline,
  not just a link), a live-updating release readiness message per release,
  slash commands (`/vaettir readiness <release>`), and an App Home tab
  showing an org's release readiness across projects. `labels: integration:slack, type:feature` — the first slice built: configurable event notifications to a channel. Reuses the exact same `Organization.slackWebhookUrl` `P7-09`'s digest already posts to (a Slack incoming webhook is bound to one channel at creation on Slack's own side, so there's no separate "channel" concept to add), gated by a new `slackEventTypes: String[]` column - a per-org subset of `P9-06`'s `WEBHOOK_EVENT_TYPES`, empty by default (no notifications) same convention as `WebhookEndpoint.eventTypes`. New `services/slackEventNotify.ts`'s `notifySlackEvent` builds a Block Kit message per event type (reusing `readinessDigest.ts`'s exact Block Kit + `fetch` pattern) and is called fire-and-forget from the same three real call sites `P9-06`'s `dispatchWebhookEvent` already fires from (`risk_flag.created` in `recommendForChange`, `compliance.sign_off_recorded` in `signOffControl`, `test_case.review_requested` in `persistReverseEngineerResult`) - a second, independent subscriber of those events, not a replacement for the generic outbound webhook system. New `organization.updateSlackEventTypes` mutation and a "Slack event notifications" section on the org settings page (checkbox-per-event-type, matching `WebhooksSection`'s existing UI pattern), gated with a plain-language note when no webhook URL is configured yet rather than hiding the section entirely. Verified for real against the live Kall project: pointed the org's real webhook config at a local receiver, subscribed to `compliance.sign_off_recorded` only, triggered a real `compliance.signOffControl` call through the actual router, and confirmed the receiver got a correctly-formatted Block Kit message with the real project name, real control title, real signer email, and real period - not a fixture. Also verified the settings UI round-trips for real in a live browser (checked a box, saved, reloaded, confirmed it persisted). All test config and data restored/deleted after verification. **Not built yet**: interactive Block Kit actions, the live-updating per-release message, slash commands, and the App Home tab - those are the ticket's own explicitly-later "grows into" phase.
- **P9-04** (M) Datadog/PagerDuty linkage: connect a production incident to the test plan/case that should have caught it, closing the loop from incident → coverage gap → new test case. `labels: integration:datadog, integration:pagerduty, type:feature`
- 🟡 **P9-05** (L) Public API + API docs (OpenAPI/tRPC-to-OpenAPI) for orgs wanting to build their own integrations. `labels: area:api, type:feature` — new [docs/API.md](docs/API.md): the real, immediately-usable version - exactly how to call the API directly over plain HTTP with an API key, without the TypeScript client, matching what `ci-integrations/*/report.sh` already does in real CI configs. Every request/response shape shown (the `GET ?input=` query form, the plain-JSON `POST` mutation form, the `result.data` success wrapper, and the `401`/`UNAUTHORIZED` error body) was verified for real against a running local server with a genuine API key, not written from the type signatures alone. A full generated OpenAPI spec (per-procedure `.meta({ openapi })` annotation across the whole ~200-procedure router) is real, invasive work across every router file and deliberately not attempted in this pass - same "broad blast radius" reasoning already applied to `P1-15`/`P12-03`'s remaining pages. **Update 2026-09-04**: a real, scoped attempt was made on the highest-value slice (the 5 procedures `docs/API.md` already calls out as worth calling externally) using `trpc-to-openapi` - annotated each with `.meta({ openapi: {...} })`, generated a real OpenAPI document (confirmed correct: all 6 real paths present, right methods/tags), then hit a genuine, reproducible third-party bug: its Fastify adapter (`fastifyTRPCOpenApiPlugin`) stack-overflows on a real request against our Fastify 5.12.1 setup (`RangeError` in `Reply.get`), confirmed via a second adapter (`node:http`) that the same router/document dispatches correctly with no recursion - isolating the bug to the Fastify adapter specifically, not the library's core. Reverted cleanly rather than shipping a workaround (a manual raw-request bridge into the production HTTP server) unattended - full writeup and the fix's likely shape in `docs/API.md`'s "What's still open" section for whoever picks this up next.
- ✅ **P9-06** (M) Webhook system (outbound) so external systems can react to platform events without polling. `labels: area:api, type:feature` — new `WebhookEndpoint`/`WebhookDelivery` models (per-org endpoints, each with its own HMAC secret shown once at creation; a full delivery-attempt log so "is this actually working" is answerable from the UI, not a mystery) and `services/webhookDelivery.ts`'s `dispatchWebhookEvent`, called fire-and-forget (matching `P5-14`'s precedent) from three real, already-existing events - deliberately a fixed literal list (`WEBHOOK_EVENT_TYPES`), not an open string, so every emitter is grep-able: `risk_flag.created` (PR-scan coverage gaps in `recommendForChange`), `compliance.sign_off_recorded` (`signOffControl`), and `test_case.review_requested` (one event per file, not per case, from `persistReverseEngineerResult` - the shared function all three reverse-engineering entry points funnel through). Each delivery is signed (`X-Vaettir-Signature: sha256=<hmac>`, same scheme as the inbound GitHub webhook verification already uses, just inverted) and a 5s timeout keeps one dead endpoint from piling up in-flight requests. New "Webhooks" section on the org settings page (create/list/delete, event-type checkboxes, a "Send test" button using the exact real dispatch path, and a delivery log viewer). Verified for real, no shortcuts: created a real endpoint pointing at a local receiver server, fired a real test delivery and independently recomputed the HMAC signature from the endpoint's own secret to confirm an exact match (not just "a request arrived"), then triggered a real `compliance.signOffControl` call and a real `agent.reverseEngineerFile` call through the actual routers and confirmed both produced real, correctly-typed deliveries at the receiver. All synthetic data (endpoint, control, sign-off, produced test case) deleted after verification.
- **P9-07** (L) Zapier/Make.com app listing, once the webhook system exists - lower priority, only worth it after real integration demand shows up. `labels: integration:zapier, type:feature`

---

## Phase 10 - Platform Hardening

Things that stop mattering less as the platform gets real users and real
compliance customers.

### Epic 10.1 - Security & compliance (of the platform itself)
- 🟡 **P10-01** (L) The platform will itself need SOC 2 (customers storing compliance evidence here will ask). Start the control implementation early - logging, access review, vendor management - not as an afterthought. `labels: compliance, type:infra` — the "access review" slice built: a real periodic-access-review feature (not a doc, actual product surface), the same kind of evidence a SOC 2 auditor asks for ("someone with admin access looked at who has access and confirmed it's still appropriate," on a recurring cadence). New `AccessReview`/`AccessReviewEntry` models (snapshot fields - email/role/seat type at review time, not FKs to `Membership` - so the record stays historically accurate even after a later role change or removal); `organization.submitAccessReview` deliberately requires covering every current member in one pass (no partial/drip review, enforced server-side by exact-set-equality against current `Membership` rows) so each completed row is genuinely a full review, not an open-ended queue; a `REVOKED` decision takes effect immediately (reuses the same "can't revoke every Owner at once" guard `removeMember`/`updateMember` already enforce). New "Access Review" page under org settings (linked from the sidebar next to Members): a table of every current member with a Confirm/Revoke decision and optional note per row, a period field, and a running history of past reviews (expandable to see every decision made). Verified for real, not just typechecked: a direct `appRouter` caller test against a throwaway org confirmed an incomplete review (omitting a member) is correctly rejected, then a real review with one CONFIRMED and one REVOKED decision correctly persisted the record AND immediately removed the revoked member's `Membership` row - not just marked it; separately, the real web UI was exercised live in the browser against the actual demo org (real review submitted, "Last reviewed 0 days ago" banner updated, past-reviews table and its expandable per-decision detail both confirmed showing the real submitted data) - left in place as a genuine first real access review for that org, not synthetic/throwaway data. **Not attempted**: logging and vendor-management, the ticket's other two named areas - `AuditLog` (P1-07) and Sentry error tracking (P10-05) already substantially cover the logging half; vendor management (a register of sub-processors/DPAs) is a compliance-ops/legal artifact, not really an engineering task, and wasn't attempted here.
- 🟡 **P10-02** (M) Secrets management audit: `ANTHROPIC_API_KEY`, GitHub App private key, DB credentials - move from `.env` to a proper secrets manager (AWS Secrets Manager / Doppler / Vault) before any production deploy. `labels: type:security, type:infra` — new [docs/SECRETS_AUDIT.md](docs/SECRETS_AUDIT.md), checked directly against the live AWS account (`aws ecs describe-task-definition`, not inferred from code): `DATABASE_URL`/`ANTHROPIC_API_KEY`/`CLERK_SECRET_KEY` are correctly in AWS Secrets Manager for both `vaettir-api` and `vaettir-web` where used; the two `NEXT_PUBLIC_*` build-time values are correctly plaintext (they're meant to ship in the client bundle). Also confirmed S3 access uses the ECS task's own IAM role (no stored access key at all - the *more* secure pattern) and CI `ApiKey`s are stored hashed, never plaintext after creation. **The one real gap**: `GITHUB_APP_ID`/`GITHUB_APP_PRIVATE_KEY`/`GITHUB_WEBHOOK_SECRET` (the App registered for `P6-01`) have real, working values in local `.env` only - never pushed to Secrets Manager or the `vaettir-api` task definition, so Phase 6 (PR scanning) is fully built but non-functional in production today. Flagged in `NEEDS_ATTENTION.md` rather than fixed directly - handling a real private key through AWS mutations needs your sign-off.
- ✅ **P10-03** (M) Dependency/vulnerability scanning in CI (`npm audit`/Snyk/Dependabot) - flag before it's a customer-facing incident. `labels: type:security, type:infra` — new `.github/dependabot.yml`: the pnpm workspace (root directory, weekly, minor/patch bumps grouped so routine version bumps don't flood the PR list while security patches still land as individual reviewable PRs), GitHub Actions, and both Dockerfiles. While wiring this up, found and fixed a real, currently-live gap: `ci.yml`'s `push` trigger was scoped to `branches: [main]`, but this repo's actual default branch is `master` (confirmed via `git remote show origin`) - the push trigger had never once fired all session; only the unscoped `pull_request` trigger ran. Fixed to `[master]`.
- ✅ **P10-04** (S) Rate limiting + abuse protection on public-facing endpoints (especially the AI reverse-engineering endpoint - it's a direct line to a paid LLM API). `labels: type:security, area:api` — `@fastify/rate-limit` registered globally (300 req/min per IP) across both the unprefixed and `/api`-prefixed route trees, a layer below (and distinct from) the AI endpoint's existing per-org business-logic limits (`P2-09`'s 200 jobs/hour, `P12-10`'s credit metering) - this guards the whole server against raw request-flooding, not just AI-call cost. `/health` is explicitly exempted (`config: { rateLimit: false }`) on both route trees so ALB health checks never trip it or get counted against real traffic. Verified for real against the local dev server: fired 320 rapid requests at a real `/trpc` endpoint - exactly the first 300 succeeded, the 301st and beyond correctly got `429`, and `/health` kept returning `200` throughout, including after the limit had tripped.

### Epic 10.2 - Observability & reliability
- 🟡 **P10-05** (M) Structured logging + error tracking (Sentry) across api/web/mobile. `labels: type:infra` — api + web done and now genuinely live in production, mobile deferred (see below). **Update 2026-09-02**: a real Sentry org/projects exist (`skald-and-stone`, `vaettir-api`/`vaettir-web`), both real DSNs are wired into the rebuilt AWS account's task definitions and the web CodeBuild project (see `docs/AWS_DEPLOYMENT.md`), and confirmed captured a real production error end-to-end during that rebuild - this is no longer "entirely inert," it's actually running. New `apps/api/src/instrument.ts` (imported first in `server.ts`, before any other module) initializes `@sentry/node` when `SENTRY_DSN` is set, inert otherwise; a Fastify `setErrorHandler` catches raw-route errors (e.g. the GitHub webhook route), and a new `reportUnexpectedTrpcError` passed to both `fastifyTRPCPlugin` registrations (unprefixed and `/api`-prefixed) reports only `INTERNAL_SERVER_ERROR` - the expected `UNAUTHORIZED`/`FORBIDDEN`/`BAD_REQUEST`/`NOT_FOUND` thrown as normal control flow throughout every router are deliberately not reported, or Sentry would be pure noise from minute one. `process.on("unhandledRejection"/"uncaughtException")` are also wired. The three in-process job pollers (`reverseEngineerWorker`, `readinessDigestScheduler`, `aiCreditGrantScheduler`) now report their previously-silently-swallowed failures too - these run unattended with zero other visibility, so a broken Slack webhook or a DB hiccup mid-poll no longer disappears without a trace; fixed a real bug found while wiring this in (`reverseEngineerWorker.ts`'s credit-check catch re-wrapped `InsufficientAiCreditsError` into a plain `Error`, which would have made the new Sentry-reporting `instanceof` check misfire and alert on ordinary "org ran out of credits" outcomes - removed the re-wrap entirely, the original error already carried the right message). Web side: `apps/web/instrumentation.ts` (server/edge runtimes) + `instrumentation-client.ts` (browser, using Next 15.3+'s native convention - no `next.config` wiring needed for client init) + `app/global-error.tsx` (catches errors the root layout itself throws, the one place Next's normal error boundaries can't reach) all initialize `@sentry/nextjs` the same DSN-gated way; `next.config.mjs` wrapped with `withSentryConfig` for future source-map upload (inert without `SENTRY_AUTH_TOKEN`, doesn't block the build). New `SENTRY_DEBUG=1` env flag logs every SDK operation, kept as a permanent tool for confirming capture is actually firing, not a throwaway. Verified for real, not just typechecked: minted a real API key through the live `apiKeys` router, fired an actual HTTP request that triggers a genuine `INTERNAL_SERVER_ERROR` (a `findUniqueOrThrow` miss on a real release id), and confirmed via `SENTRY_DEBUG=1` that Sentry's logger printed `Captured error event` for it; separately loaded the web app in a real browser and confirmed `window.__SENTRY__` was live and attempting real network sends. **Entirely inert in production today** - no Sentry account/project exists yet, so there's no DSN to set; wiring the real DSN into Secrets Manager + both ECS task definitions needs your sign-off (production secret, same shape as the still-open GitHub App gap) - see `NEEDS_ATTENTION.md`. **Mobile not attempted**: `@sentry/react-native` needs an Expo config plugin + native rebuild that can't be verified without a live device/emulator, same limitation noted throughout this session's other Phase 8 mobile work.
- ✅ **P10-06** (M) Metrics/tracing (OpenTelemetry) - especially around AI agent latency/cost, since that's the most expensive and most failure-prone path. `labels: type:infra` — reused `P10-05`'s already-shipped Sentry pipeline rather than standing up a second, parallel OpenTelemetry SDK/collector: `@sentry/node` v10 is itself OTel-based and already auto-instruments HTTP/Prisma once `SENTRY_DSN` is set, so a separate tracing stack would have been redundant infrastructure for no new capability. The genuine gap was AI-specific: nothing captured per-operation latency or token cost for the single most expensive, most failure-prone path in the platform. New `packages/ai-agent/src/tracing.ts`'s `traceAnthropicCall` wraps every one of this package's 8 real `client.messages.create` calls (reverse-engineer, risk assessment, failure classification, QA strategy generation, test-case generation, requirement extraction, framework-heuristic inference, test-plan recommendation) in a Sentry span named after the same operation key `P12-10`'s AI-credit costs already use, with model/input-token/output-token as span attributes - so cost and latency show up against the same vocabulary the credit ledger already speaks. `Sentry.startSpan` is a safe no-op without an active client, matching `P10-05`'s "inert until `SENTRY_DSN` exists" contract exactly - no caller needed to change. Verified for real against a live Anthropic call (not mocked): with no DSN configured, `assessTestCaseRisk` still returned a correct real result, proving the wrap doesn't perturb normal operation; with a DSN configured and Sentry debug logging on, confirmed via the SDK's own debug output that a real `ai.anthropic` span named `assessTestCaseRisk` was started and finished around a real Claude call - and, as a bonus neither planned nor coded, Sentry's own HTTP auto-instrumentation automatically nested the real `POST https://api.anthropic.com/v1/messages` request as a child span, meaning this also gets network-level timing for free. Full `pnpm test` (40 tests) still green after the change. Actual dashboards/alerts on this data need the same real Sentry DSN `P10-05` is still waiting on - see `NEEDS_ATTENTION.md`.
- ✅ **P10-07** (S) Uptime/health checks + alerting for the API and the job worker (Phase 2's queue). `labels: type:infra` — the health-check half: new `GET /health/detailed` (deliberately separate from the plain `/health` the ALB/ECS health check uses - that one stays a pure liveness probe on purpose, since a transient DB blip making it fail too would cycle the whole task, which is worse than serving degraded for a moment). It checks real DB connectivity (`SELECT 1`) and, new `services/heartbeat.ts`, whether each of the three in-process job pollers (`reverseEngineerWorker`, `readinessDigestScheduler`, `aiCreditGrantScheduler` - Phase 2's queue plus the two other unattended pollers built later this session) has ticked recently relative to its own known interval - `recordHeartbeat` called at the top of each poller's tick function. Always returns `200` with `{ healthy, db, pollers }` rather than ever 5xx-ing on its own check, so an external uptime monitor alerts on the JSON payload rather than misreading "the detailed check itself broke" as "the whole API is down." Verified for real against a live local server: DB connectivity correctly reported `ok: true`, and two pollers that had genuinely ticked showed real fresh timestamps while the third (`readinessDigestScheduler`, whose 15-minute interval has no immediate first-tick unlike the other two) correctly reported `stale: true, lastTickAt: null` - real, accurate detection, not a hardcoded green response. Worth knowing: `readinessDigestScheduler` will show `stale: true` for up to 15 minutes after every fresh deploy/restart by design (it doesn't tick immediately on startup) - not a bug, just don't page on that specific poller within 15 minutes of a deploy. **Update 2026-09-02**: the alerting half is done too - a real Sentry uptime monitor (`skald-and-stone` org, `vaettir-api` project, monitor id `10245987`) polls `https://vaettir.skaldandstone.com/api/health/detailed` every 5 minutes and confirmed `uptimeStatus: "ok"` on its first real check; a failing check creates a Sentry issue (org members get Sentry's default new-issue notifications, same channel as every other error this app now reports).
- 🟡 **P10-08** (M) Database backup/restore runbook + tested recovery process - before there's real customer data to lose. `labels: type:infra` — new [docs/DATABASE_BACKUP_RESTORE.md](docs/DATABASE_BACKUP_RESTORE.md): current real state checked via `aws rds describe-db-instances` (1-day automated backup retention, no Multi-AZ), the actual documented RDS point-in-time and snapshot restore procedures, and the cutover steps specific to this deployment (updating the `vaettir/database-url` secret + forcing a new `vaettir-api` ECS deployment). Found the retention is thin for a now-real production app (a mistake discovered more than a day later has no recovery point) and recommended bumping it to 7 days - a safe, reversible, no-downtime RDS setting change. **Update 2026-09-02**: applied, with your go-ahead, against the rebuilt `vaettir-postgres` instance - `aws rds modify-db-instance --backup-retention-period 7 --apply-immediately` confirmed `BackupRetentionPeriod: 7` with no instance downtime (`DBInstanceStatus` stayed `available` throughout). **Not done**: an actual live restore drill (spinning up a real restored instance costs real time/money and doesn't change what the documented procedure is - worth doing once deliberately, not unattended) and Multi-AZ (a different problem - AZ-outage failover, not bad-data recovery - not urgent for a single-user app today).

### Epic 10.3 - Deployment & DX
- 🟡 **P10-09** (M) Production deployment pipeline (pick a target: Vercel for web, Fly.io/Render/ECS for api+worker, EAS for mobile) with staging + prod environments. `labels: type:infra` — found substantially already satisfied, via a different route than originally imagined: this ticket predates the real AWS account rebuild done later this session after production went down (an unrelated AWS org removal, not planned deployment-pipeline work), which picked ECS for both web and api and is genuinely live - `scripts/deploy-aws.sh` drives a real `git archive` → S3 → CodeBuild → ECR → ECS pipeline (see `docs/AWS_DEPLOYMENT.md` for the complete architecture: VPC, ALB, CloudFront, RDS, Secrets Manager, all of it), fronted by a real domain (`vaettir.skaldandstone.com`) with a Sentry uptime monitor (`P10-07`). **Not built**: a separate staging environment - today there's exactly one AWS account/stack and it IS production, so every deploy this session has gone straight there; standing up a genuinely separate staging stack (its own RDS instance, ECS cluster, domain) is a real recurring-cost infrastructure decision, not something to do unattended. EAS for mobile is also still open, tracked separately as `P8-07` (blocked on real Apple/Google developer accounts).
- ✅ **P10-10** (S) Full CI: extend the current typecheck-only workflow to run lint, unit tests, and (once P1 auth exists) integration tests against a real Postgres service container. `labels: type:infra, dx` — `.github/workflows/ci.yml` was already ahead of this description (typecheck, lint, AND a `pnpm test` step, against a real Postgres service container) - it just had nothing for `pnpm test` to actually run, since no package had a `test` script until `P10-11`. That step is real now. Integration tests against real auth/DB are done too, later the same night: `P10-11`'s `trpc.integration.test.ts` (RBAC/staff-gate) plus the `importJobs`/admin-ownership router suites all run for real against CI's Postgres service container - closing the exact gap this ticket's own note flagged as still open, once the CI migrate/seed steps existed for them to run against.
- ✅ **P10-11** (M) Unit + integration test suite for the API routers and the AI agent's parsing logic - the platform that manages tests should not itself be undertested. `labels: type:testing, dx` — test infra established (Vitest, wired into the root `pnpm test` → `turbo run test` pipeline that already existed but had nothing to run), plus real unit tests for the pure, dependency-free logic built this session: `packages/core`'s seat-limit logic (`canAddSeat`, `minimumTierForSeatCount`), risk fallback scoring, and - the highest-value coverage here - the Gherkin and Postman collection parsers (background-folding, Scenario Outline expansion, tag scoping, nested-folder recursion, malformed-input handling); `apps/api`'s PR-scan path-severity matching and the `P7-03` readiness scoring formula (weighting, the CRITICAL-forces-BLOCKED rule, the score-never-goes-negative floor). Router-level integration tests now added too: new `apps/api/src/trpc.integration.test.ts` hits a real Postgres connection (not a mock) through the actual `appRouter`, proving the real RBAC gate - a user with no org membership gets `FORBIDDEN`, a `VIEWER` can read but not create (`EDITOR`-gated), an `EDITOR`'s create actually persists a real row, an unknown `projectId` is `NOT_FOUND` rather than a silent empty result, and `P13-01`'s staff gate correctly rejects a non-staff email (including a customer `OWNER` - a real membership role does NOT grant staff access) and admits a real `@skaldandstone.com` one. Everything the suite creates is scoped under one throwaway org/users and deleted in `afterAll`. **Caught and fixed two real infrastructure bugs while building this**: (1) the first draft's cleanup had a latent unscoped-delete hazard - if `beforeAll` threw partway through setup, `afterAll` would run `deleteMany` with an unset filter variable, which Prisma treats as "no filter" (i.e. every row in the table); it surfaced immediately as a real near-miss against this machine's live dev database (an unrelated FK constraint fortunately rolled the whole delete back before anything was actually lost, confirmed by an unchanged row count afterward) and is now fixed by guarding every cleanup step on the real captured id, never an assumed-set variable; (2) `turbo.json`'s `test` task was silently stripping `DATABASE_URL` from the child process before this - no previous test needed a DB connection, so the gap was invisible until now. Fixed via `env: ["DATABASE_URL"]` on the task, plus new "Apply database migrations"/"Seed reference data" CI steps (`.github/workflows/ci.yml`) since CI's Postgres service container starts blank and this suite depends on the seeded `PlanTier` rows existing. Coverage extended to two more real routers: `importJobs.integration.test.ts` (the CSV field-mapping preview/commit flow from `P11-01`/`P11-02`, which previously had only an ad-hoc throwaway verification script during development, no permanent regression coverage - auto-suggestion, live re-mapping, a real commit creating real `TestCase`/`ImportJob` rows, and the "title must be mapped" rejection) and `admin.ownership.integration.test.ts` (`P13-05`'s suspend/reactivate/transfer, in its own isolated org so a suspension in one test can never leak into another test's expectations - suspend genuinely blocks project access, staff stays exempt so it can be lifted, double-suspend/double-reactivate are rejected, and a transfer correctly promotes/demotes both members). 48 total tests, all passing for real, verified via a full clean (non-cached) `pnpm test` from the repo root. AI-agent LLM-calling functions still deliberately uncovered - no meaningful way to unit-test a live model call; those stay covered by this session's live-data verification scripts.
- ✅ **P10-12** (S) Local dev docs: one-command local setup (`docker-compose` for Postgres + Redis once the job queue exists), contribution guide. `labels: dx` — new root `docker-compose.yml` (Postgres only - no Redis yet since there's still no real queue, just the in-process job pollers) and `CONTRIBUTING.md` (setup, pre-PR checklist, and the conventions worth knowing: data-not-enums plan types, every router calling `requireProjectAccess`, AI credits metered at the call site, append-only audit models). Also fixed a real, stale inconsistency found while writing this: `.env.example` and `.github/workflows/ci.yml` still referenced the project's old pre-rename database name (`test_case_intelligence`/`qi_platform`) - both now say `vaettir`, matching the real local dev database's actual name. Not run against the live dev environment tonight - this machine's Postgres is already a native (non-Docker) install with real data, and starting a second Postgres via compose would either conflict on port 5432 or require care to test against safely; the compose file itself is straightforward enough (a single `postgres:16` service matching CI's shape) that it didn't seem to need risking the running environment to prove out.
- **P10-13** (M) Design system / shared UI package (`packages/ui`) once web has enough screens that inconsistency becomes visible - don't build this speculatively before then. `labels: area:web, type:tech-debt`
- ✅ **P10-15** (M) ESLint is a documented no-op today: CI's "Lint" step (`pnpm lint` → `turbo run lint`) runs and reports success, but zero packages define a `lint` script, so it lints nothing - discovered overnight while checking why `pnpm lint` produced "0 successful, 0 total" instead of actually running anything. `prettier` is an installed root devDependency with no config file either, so formatting isn't enforced anywhere. `labels: type:infra, dx` — deliberately deferred overnight as needing exactly what happened next: James actively present in the same session to help triage whatever a first real lint run turned up, rather than a human distinguishing real problems from noise being invented after the fact. Built with him around. New root `eslint.config.mjs` (ESLint 9 flat config, `typescript-eslint` recommended everywhere, `eslint-config-next` layered on `apps/web` only via `FlatCompat`) plus a real `"lint": "eslint ."` script added to all six packages so `turbo run lint` actually invokes something. Deliberately not type-aware yet (no `parserOptions.project`) - the non-type-checked recommended rules already catch the real stuff, and type-aware linting across a monorepo this size is its own setup/perf cost worth a separate pass once this baseline is proven. First real run across the whole codebase found remarkably little: 3 warnings in a test file written earlier the same night (unused seed variables - fixed), 1 stray unused `eslint-disable` comment in `packages/db` (fixed), and in `apps/web` - the only package with any real volume - a genuine, fixed finding (six `<a href="/...">` internal links that should have been Next's `<Link>`, losing client-side navigation otherwise: `layout.tsx`'s nav brand, the homepage CTA, `Sidebar.tsx`'s "All projects" back-link, and one in the test-cases empty state) plus one unused type alias and one more stray `eslint-disable`, both fixed, alongside 32 purely-cosmetic `react/no-unescaped-entities` findings (an unescaped `'`/`"` in JSX text renders completely correctly in every browser - zero behavioral difference either way) and 3 `react-hooks/exhaustive-deps` warnings that are real judgment calls (adding the "missing" dependency risks introducing an actual re-render bug if the effect was written to run once on purpose) - both categories deliberately left as warnings, not silently fixed or suppressed, for a real human pass rather than a mechanical one. `react/no-unescaped-entities` downgraded from `eslint-config-next`'s default `error` to `warn` specifically so `pnpm lint`/CI goes green today instead of immediately blocking every future PR on 32 pre-existing, untriaged, purely-cosmetic findings - re-promote once a pass through them lands. Verified for real: a deliberately-broken scratch file confirmed the config actually catches real violations (not silently no-op), full `pnpm typecheck` and `pnpm test` (51 tests) both clean after every fix, and a final `pnpm lint` from the repo root exits 0 (0 errors, 35 warnings) across all six packages.
- ✅ **P10-14** (M) Vaettir's own E2E suite, dogfooded as real test cases: a real Playwright suite covering the platform itself, reverse-engineered into a new **"TCM"** project inside Vaettir (Test Case Management - Vaettir tracking its own testing) via the exact same pipeline any customer's repo uses. `labels: area:web, type:testing` — `apps/web/e2e/` (13 spec files, ~75 real scenarios) covers essentially every surface built this session: auth, projects, test cases (BDD authoring, quick-add, bulk ops, search, CSV import), test plans, requirements, AI reverse-engineering (paste/background-job/Gherkin/Postman/custom-framework-heuristic), compliance (mapping, evidence, sign-off, CSV export), test strategy (risk assessment, change-impact, PR scan policy), release readiness (criteria, risk flags, gates, trend charts), the org dashboard, test runs (result linking, self-healing classify/approve/reject), the audit log, members/invites/seats/plan switching, and AI credits. A `global-setup.ts` signs in once via the real Clerk form and reuses the session across specs (see `apps/web/e2e/README.md` for the `CLERK_TEST_EMAIL`/`CLERK_TEST_PASSWORD` needed to run it - not something to fabricate credentials for). All 13 spec files were then reverse-engineered for real (`agent.reverseEngineerFile`, `persist: true`, real calls) into the new **TCM** project (id `cmtbaedfd0001vdp0c0i5lgu2`) - Playwright is a native `FrameworkFamily` (deterministic structural extraction, not a pure AI read), and every file correctly detected as such, producing 75 accurate, source-linked `TestCase`s (`origin: AI_REVERSE_ENGINEERED`, `reviewStatus: PENDING_REVIEW` - these genuinely need a human review pass like any other AI extraction, not a free pass). Required a manual `ADJUSTMENT` transaction (100 credits) on the org's AI-credit ledger to cover this batch, since it's real internal dogfooding work, not customer spend against the monthly grant - logged plainly on the ledger itself, not hidden. **Not covered**: mobile (Playwright drives a browser, not Expo/RN), the GitHub App webhook path (needs a real delivered webhook, covered by `P6-01`'s own verification approach instead), and exact AI-output wording (checks that AI features respond correctly, not fixed phrasing - same reasoning as `P2-10`'s prompt regression suite). The suite itself has not been run end-to-end tonight - no Clerk test credentials available in this environment; structurally verified via a clean `apps/web` typecheck and the real reverse-engineering pass, not a live Playwright run.

---

## Phase 11 - Migration & Import

We're asking teams to switch off an entrenched incumbent (TestRail,
Zephyr, qTest, Xray, PractiTest - see
[COMPETITIVE_ANALYSIS.md](COMPETITIVE_ANALYSIS.md) for what each does
well and what their users complain about). The switching cost is real
existing test cases, real suite/plan structure, and real run history -
a migration path that loses any of those isn't a migration path.
`ReverseEngineerJob` (Phase 2) is the template: a typed job with status
tracking that a background worker processes; import jobs follow the
same shape.

### Epic 11.1 - Import pipeline foundation
- ✅ **P11-01** (L) Generic `ImportJob` model + pipeline: source enum, status tracking, field-mapping config stored as JSON, dedupe-by-external-id - the shared foundation every source-specific importer plugs into. `labels: area:db, area:api, type:feature` — new `ImportJob` model (`source` enum seeded with `CSV`/`TESTRAIL`/`ZEPHYR`/`XRAY`/`QTEST` so later importers slot into the same tracking without a schema change; `fieldMapping` Json stores the exact user-confirmed column mapping, reusable by `P11-11`'s future incremental re-sync) and `importJobs` router (`previewCsv`/`previewWithMapping`/`commitCsv`/`list`). CSV is the first source wired all the way through; TestRail/Zephyr/Xray/qTest (`P11-03`-`P11-06`) still need their own fetch+map logic, but the job-tracking/audit/history layer they'll write into already exists. Dedupe-by-external-id itself isn't implemented yet - CSV rows have no natural external id to dedupe on, so that piece is deferred to whichever `P11-03`-`P11-06` importer needs it first (a real source API's id, not a guessed shape).
- ✅ **P11-02** (M) Field-mapping UI: preview parsed records against target `TestCase`/`TestPlan` fields, let the user adjust mapping and custom-field targets before committing - nothing writes until confirmed. `labels: area:web, type:feature` — new `/projects/[projectId]/import` page (linked from the sidebar): upload any CSV (not the fixed header set `P11-07`'s `testCases.importCsv` requires), get a best-guess column-to-field mapping, adjust any of the six mappable fields (title/given/when/then/priority/tags) via dropdown with a live-updating preview table, then commit. New `services/csvFieldMapping.ts` (`inspectCsv` for the header/suggestion pass, `mapCsvRows` shared by both the preview and the real commit so they can never disagree about what a mapping produces). Custom-field-target mapping (beyond the six built-in TestCase fields) not attempted - no generic custom-field system exists on `TestCase` yet to map into. Verified for real end-to-end, not just typechecked: backend cross-checked via `appRouter.createCaller` against a real project (auto-suggestion correctly matched 5/6 columns and correctly left "title" unmapped since the source header was "Test Name," not an exact alias - proving the confirmation step is load-bearing, not decorative); then, since this session had no prior browser-login capability, used a real demo Clerk login (coordinated from another session working the same account overnight) to click through the actual page in a browser: uploaded a real File via a genuine `change` event, watched the mapping dropdowns and preview table update live, committed, and confirmed the 2 real `TestCase` rows landed correctly (origin `IMPORTED`) on the project's real Test Cases page. Verification org/project deleted after.

### Epic 11.2 - Source-specific importers
- **P11-03** (L) TestRail importer: cases/suites/sections → `TestPlan`/`TestCase`, runs/results → `TestRun`/`TestResult`, via TestRail's REST API (`get_cases`/`get_suites`/`get_runs`/`get_results`). Build this first - it's the best-documented API in the category and the lowest-risk way to prove the import pipeline itself. `labels: area:api, integration, type:feature`
- **P11-04** (L) Zephyr Scale (Jira) importer: test cases/cycles/executions via the Zephyr Scale REST API v2, folder structure → `TestPlan` hierarchy. Highest-complaint-volume source platform (see competitive analysis), so highest-value migration target despite a less uniform API than TestRail's. `labels: area:api, integration, type:feature`
- **P11-05** (M) Xray (Jira) importer: bulk CSV/JSON export via a JQL query → `TestCase`/`TestPlan`. Automation *result* ingestion is already covered by Phase 5's generic Cucumber/JUnit/NUnit ingestion - that's the same format Xray itself consumes. `labels: area:api, integration, type:feature`
- **P11-06** (L) qTest importer: `test-cases`/`test-runs`/`test-logs` resources via the qTest REST API v3, module hierarchy → `TestPlan` hierarchy. `labels: area:api, integration, type:feature`
- ✅ **P11-07** (M) Generic CSV/Excel importer with a reusable column-mapping template - the catch-all for PractiTest, TestLink, and any spreadsheet-tracked suite without a first-class importer. `labels: area:api, area:web, type:feature` — CSV half built (Excel/.xlsx not attempted - would need a real parsing dependency, CSV export covers the realistic "get data out of a spreadsheet tool" path already). New `services/testCaseCsvImport.ts` (RFC 4180 parsing, same approach as the existing compliance-controls CSV import) recognizes a fixed common header set (`title`/`given`/`when`/`then`/`priority`/`tags`, case-insensitive) rather than a full drag-and-drop field-mapping UI (`P11-02`, separate ticket, not attempted) - a reasonable, immediately-usable middle ground. Multi-step cells are `|`-delimited. A row missing `title` or any of `given`/`when`/`then` is skipped with a specific reason and 1-indexed row number rather than failing the whole import; imported cases land with `origin: IMPORTED` (skips the AI trust gate, same as the existing Gherkin/Postman importers). New "Import CSV" button + modal on the Test Cases page. 7 real unit tests for the parser (well-formed rows, multi-step splitting, priority defaulting/validation, missing-title and missing-BDD-step skip cases with correct row numbers, the no-title-column hard error, empty/header-only input) plus end-to-end verification against the live Kall project: a real 4-row CSV correctly imported 2 valid cases and skipped 2 invalid ones with the right reasons, confirmed the created rows' `origin`/`reviewStatus`/parsed fields in the database, and confirmed a CSV with no `title` column is rejected outright. All synthetic data deleted after verification.
- ✅ **P11-12** (S) General test case CSV export, to match `P11-07`'s import. Found via the 2026-08-27 competitor parity audit: every researched competitor supports both directions, and `importCsv` had no matching export. `labels: area:api, area:web, type:feature` — new `testCases.exportCsv` returns the exact same field shape `importCsv`'s header set expects (`title`/`given`/`when`/`then`/`priority`/`tags`), so export → import is genuinely round-trippable, not just data-out with no way back in. CSV construction happens client-side (new shared `lib/csv.ts`, extracted from `P3-04`'s compliance-report export so both pages use the identical RFC 4180 quoting rule instead of two copies that could drift). New "Export CSV" button on the Test Cases page. Verified for real against the live Kall project: exported all 117 real test cases, fed the resulting CSV back through the actual `parseTestCaseCsv` import parser, and confirmed the round-trip produced exactly 117 cases with zero skips and byte-exact `given`/`when`/`then` content on a spot-checked case.

### Epic 11.3 - Historical run data
- ✅ **P11-08** (L) Historical run-data backfill: import pass/fail execution history with *original* timestamps (not import date), so pass-rate trends, flaky-test detection (`P5-05`), and release-to-release charts (`P7-07`) aren't empty or skewed on day one. A cold-start dashboard with no history is itself an adoption blocker. `labels: area:api, type:feature` — the earlier pass this session fixed the actual blocker (`testRuns.ingestJUnit` hardcoded `startedAt`/`finishedAt` to `new Date()`) but deliberately left the guided UI itself open. Built now: a new "Backfill historical test run results" section on the project's Import page (`apps/web/app/projects/[projectId]/import/page.tsx`) - upload one or more JUnit XML files, set each one's real historical date (and an optional real commit SHA, defaulting to a clearly-synthetic placeholder rather than a fabricated hash) in a small table, then run the backfill; each file calls the existing `testRuns.ingestJUnit` with its own `startedAt`/`finishedAt`, showing live pass/fail/skip counts per file as it completes. Building this immediately caught a second, more fundamental bug in the same feature: `startedAt`/`finishedAt` were typed `z.date()`, which only round-trips correctly with a superjson transformer - this API has none, so a `Date` sent over the wire (from this new browser client, or any future curl-based CI script) arrives as a plain string and was rejected outright. Never caught before because no caller - UI or CI script - had ever actually exercised these optional fields until this UI became the first one to. Fixed by switching to `z.coerce.date()`. Verified for real, not just typechecked: seeded a throwaway org/project with a real test case linked via `TestCaseSource.externalTestId`, drove the actual browser UI (uploaded two real JUnit XML files, one PASS dated 2026-06-01 and one FAIL dated 2026-07-15), and confirmed directly against Postgres afterward that both `TestRun` rows persisted their real distinct historical dates (not the import moment) and both results correctly matched to the real seeded test case by external ID. All throwaway data deleted after verification.

### Epic 11.4 - Migration UX
- ✅ **P11-09** (M) Post-import diff/summary report: what imported cleanly, what was skipped or ambiguous and needs manual review, what dedupe collisions were found. `labels: area:web, type:feature` — was substantively satisfied for the one importer that existed at the time (`P11-07`'s bespoke CSV modal), then genuinely closed out once `P11-01`'s generic `ImportJob` pipeline landed the same night: the new `/import` page's preview step shows exactly what would be created vs. skipped (with per-row reasons) before anything commits, the post-commit result repeats the same createdCount/skipped summary, and a persistent "Import history" list (backed by the real `ImportJob` table, not a one-off mutation's return value) shows every past import's status/counts - a genuinely reusable report shape any future source-specific importer (`P11-03`-`P11-06`) plugs into automatically, not bespoke to CSV. Dedupe-collision reporting still doesn't apply - no importer here dedupes by external id yet (CSV rows have none to dedupe on) - revisit once `P11-03`+ needs it.
- **P11-10** (M) "Migration assistant" wizard: connect source credentials → scope preview (counts of what will import) → field mapping → import → diff report, as one guided flow rather than disconnected tools. `labels: area:web, type:feature`
- ✅ **P11-11** (S) Import job re-run/incremental sync: re-running an import against the same source only pulls new/changed records (reuses `P11-01`'s external-id dedupe) - supports running in parallel with the old tool during a phased migration instead of forcing a hard cutover. `labels: area:api, type:feature` — the actual blocker this ticket was waiting on (`P11-01`'s own note: "dedupe-by-external-id isn't implemented yet - CSV rows have no natural external id") is resolved by making it opt-in rather than assumed: a new "External ID" target field in the CSV mapping (`csvFieldMapping.ts`'s `TARGET_FIELDS`), deliberately given no header-alias auto-suggestion (unlike every other field) since guessing wrong here changes CREATE-vs-UPDATE behavior, not just which text lands where - the user must map it on purpose. Once mapped, `commitCsv` matches each row to a `TestCaseSource.externalTestId` from a prior run of the same import and updates that `TestCase` in place instead of duplicating it - the same update-vs-create split `reverseEngineerPersist.ts` already uses for a re-scanned source file, just keyed by a spreadsheet row id instead of a file/function name. Keys are namespaced per-project (`csv:<projectId>:<rawId>`) since `externalTestId` is globally unique across the whole platform but a spreadsheet's own ids (e.g. "TC-001") are only ever meant to be unique within one project's own source file - a real correctness bug this design avoids, not a hypothetical one. New `ImportJob.updatedCount` alongside the existing `createdCount`/`skippedCount` so a re-run's history entry shows "created 1, updated 1" rather than looking like a partial failure. Verified for real, twice over: a permanent integration test (`importJobs.integration.test.ts`) commits a CSV, re-commits a modified version with the same external ids, and confirms exactly one `TestCase` row exists afterward with the updated title, not two; separately, the real browser UI was exercised end-to-end against the live demo project - uploaded a real CSV via a genuine `change` event, mapped "External ID" to its id column, committed, then uploaded a second CSV changing one title and adding a new row, confirmed the UI reported "Imported 1 test case(s), updated 1 existing case(s)," and confirmed on the actual Test Cases page that the updated row shows its new title with no duplicate left behind. All verification data deleted after.

---

## Phase 12 - Billing, Plans & Seats

Seat-based SaaS pricing. Schema already landed (`PlanTier`, `Membership`
with `seatType: FULL | READ_ONLY`, `Organization.planTierId`,
`Organization.dataRetentionYears`) and seeded with the tier boundaries as
specified: **Free** (≤3 full seats, 0 read-only seats), **Team** (4–50 full
seats, 10 included read-only seats), **Business** (51–75), **Corp** (76+).
Per-seat pricing and AI credit grants are now seeded with a **proposed**
(not yet business-signed-off) structure - see `PRICING.md` and Epic 12.4
below; `packages/core/src/plan.ts` already has the pure seat-limit logic
(`canAddSeat`, `minimumTierForSeatCount`) ready for
whatever billing provider gets wired in.

### Epic 12.1 - Seat management
- ✅ **P12-01** (M) Seat management UI: org settings page listing members, their role, and seat type (full/read-only), with add/remove/change-seat-type actions gated through `canAddSeat`. `labels: area:web, type:feature` — found already fully built during an overnight roadmap audit (never marked done): `/settings/members` lists every member with editable role/seat-type dropdowns and a remove action, all backed by `organization.listMembers`/`updateMember`/`removeMember`.
- ✅ **P12-02** (M) Invite flow: invite by email to a specific role + seat type; invite acceptance creates the `Membership` only after `canAddSeat` passes, with a clear "seat limit reached, upgrade to invite more people" state rather than a silent failure. `labels: area:web, area:api, type:feature` — also found already built in the same audit: invite modal on `/settings/members` generates a real link (`/invite/[token]`), `inviteMember`/`acceptInvitation` both call `canAddSeat` (checked at invite time AND again at accept time, since usage can change in between) and surface a specific "seat limit reached" `TRPCError` rather than a silent failure, and pending invitations list with revoke.
- ✅ **P12-03** (S) Read-only seat UI enforcement: a `VIEWER`-role, `READ_ONLY`-seat member should not see edit/create affordances anywhere in the UI, not just be blocked server-side - the seat type is a product experience, not only a permission check. `labels: area:web, type:feature` — foundation + the two highest-traffic surfaces done: `organization.mine` now returns the calling user's own `role`/`seatType` per org (read off `ctx.user.memberships` already on context, no extra query), and new `lib/membership.ts`'s `isReadOnlySeat()` is the one place every page checks (`READ_ONLY` seats can only hold `VIEWER`, enforced server-side already, so seat type alone is sufficient - no `READ_ONLY`+`EDITOR` case to also handle). Wired into the Test Cases page (create/import/bulk-ops/reverse-engineer-scan links hidden, plus a `readOnly` prop threaded into the shared `TestCaseDetailContent`/`ComplianceControlsSection` components gating Edit/Approve/Reject/Assess-risk/Map-control) and the Test Plans list page (create + AI-generate hidden). `TestPlanDetailContent` (the actual plan-editing surface) now done too: name/description/status render as plain text instead of inputs, the custom-fields form and Save button are hidden entirely, acceptance criteria show their status as plain text with no status-select/remove/add-new controls, and the strategy-link section keeps its read-only "linked plans" list visible but hides the link/unlink picker - threaded through both the drawer (`test-plans/page.tsx`, already had `readOnly` computed) and the standalone `/test-plans/[id]` deep-link page (didn't have the seat check at all before now). The release readiness page is done too: status shows as plain text instead of a select, the Detach button and the whole attach-a-plan row are hidden, a manually-set (non-auto-computed) criterion's status renders as plain text instead of a dropdown, and the risk flag Resolve/Reopen button is hidden - the "show resolved" filter checkbox and the interactive HTML/Markdown snapshot exports stay visible since neither mutates anything. Compliance is done: "+ New framework"/"Import controls"/"+ Add control"/"+ Map a test case" are all hidden, and the evidence/sign-off drawer keeps its real history visible while hiding the "Record evidence" and "Sign off" forms - `Export CSV` stays visible since it doesn't mutate anything. Reverse-engineer is done too, but differently from the others: the entire page is write-only functionality (submit/scan/upload/import, nothing there is meant to be viewed passively), so instead of gating a dozen individual controls the whole action-panel stack is hidden behind one notice, keeping only the real job history and AI-edit-feedback sections visible. **Still open**: org settings - lower priority than the others, since every mutation there already requires `ADMIN`+ `OrgRole` server-side (well above anything a `READ_ONLY`/`VIEWER` seat can ever hold), so the seat-type gate would be belt-and-suspenders on top of an already-real block, not a new hole; also a large (978-line) page mixing billing/digest/staff concerns, deliberately not rushed. Verified for real in a browser each time (not just via the router): a genuine `READ_ONLY`/`VIEWER` membership inserted into a real org, logged in as the demo Clerk account, confirmed every write affordance was actually gone from each rendered page. **Caught a real bug while building the reverse-engineer page's gate**: the six write-action sections (scan repo, zip upload, paste-a-file, Gherkin import, Postman import, teach-a-framework) are sibling elements, not nested inside one parent `<div>` - the first pass wrapped only the opening tag of the first section, silently leaving the other five visible regardless of `readOnly`. Caught via `tsc` (a stray unclosed JSX fragment) before it was ever shipped, fixed by wrapping all six in a `<>...</>` fragment inside the one conditional. Verification memberships deleted after every pass.

### Epic 12.2 - Plan lifecycle
- ✅ **P12-04** (M) Plan upgrade/downgrade flow: changing `Organization.planTierId`, validated against current seat counts (can't downgrade below what's actually seated - surface which seats would need to be removed first). `labels: area:api, area:web, type:feature` — new `organization.listPlanTiers`/`changePlanTier`; a downgrade that would leave the org over the target tier's full- or read-only-seat cap is refused with the exact overflow count needed before it'll go through, not a generic error. Plan dropdown added to the seat-usage panel on `/settings/members`. Verified for real against the live (over-limit) Skald and Stone org: a downgrade to Free was correctly refused ("remove 3 full seat(s) first (currently 6 full / 0 read-only seated)"), a switch to Team correctly succeeded since 6 seats fits Team's 4-50 range, and `seatUsage` reflected the new tier immediately afterward. Org plan tier restored to its original value after verification - not left changed, since which real tier the org should actually be on is James's call (flagged in `NEEDS_ATTENTION.md`).
- **P12-05** (L) Payment provider integration (Stripe is the default assumption pending a final decision) once pricing is set: subscription creation, seat-count-driven quantity updates, webhook-driven plan sync so `PlanTier` stays the source of truth for entitlements while the provider stays the source of truth for money. `labels: area:api, integration, type:feature`
- ✅ **P12-06** (S) Usage/seat-count dashboard for org admins: current seats used vs. included at this tier, a clear "you're at 9/10, next seat requires Team" style prompt before someone hits a wall mid-invite. `labels: area:web, type:feature` — new `organization.seatUsage` query (full/read-only seats used vs. included, plus `minimumTierForSeatCount` telling you exactly which tier one more full seat would require) and a summary panel on `/settings/members` above the member list. Verified against the live Skald and Stone org - and in the process discovered it's already on the Free tier with 6 full-seat members against Free's cap of 3 (the seat gate only blocks *new* additions, it doesn't retroactively enforce against already-existing seats, so this isn't a bug, just real data worth a look - flagged in `NEEDS_ATTENTION.md`). Also confirmed the "needs upgrade" hint correctly names the next tier when temporarily forced onto Free with real seat counts.

### Epic 12.4 - AI credits (metered AI usage, on top of per-seat pricing)
- ✅ **P12-10** (L) AI credit ledger + per-operation metering: `AiCreditTransaction` (append-only, same auditability reasoning as `AuditLog`), `PlanTier.includedAiCreditsPerMonth`, `services/aiCredits.ts` (`chargeAiCredits`, `getAiCreditBalance`, `grantMonthlyCreditsIfNeeded`), and a `reverseEngineerWorker.ts`-style in-process monthly grant scheduler. `labels: area:api, area:db, type:feature` — wired into every real AI call site (`reverseEngineerFile`, the reverse-engineer job worker that `submitJob`/`scanRepo`/`uploadZip` all feed into, `inferCustomFrameworkHeuristic`, `assessTestCaseRisk` single + batch, `recommendTestPlansForDiff` on both the manual Test Strategy path and the automatic PR-scan webhook, `generateQaStrategyDraft`) - each charges a flat per-operation credit cost before calling the model and refuses the call with a clear error when the org's balance is insufficient. Flat costs (not exact token metering, since no `@vaettir/ai-agent` function currently returns token usage) sized to ~$0.01/credit of assumed Claude Sonnet-class API cost with ~2x headroom - see [PRICING.md](PRICING.md) for the full per-operation math. New "AI credits" section on the org settings page (balance + recent activity) and `organization.aiCreditStatus` query. Verified for real against the live Kall org's database: monthly grant lands the tier's full amount and is idempotent across repeated calls in the same month, a real charge deducts correctly and is visible in the transaction history, and draining the balance to zero correctly rejects the next charge attempt with a specific "needs N, has 0" error rather than silently proceeding or crashing. Real per-seat prices and AI credit grants for all four tiers are seeded and live (see `PRICING.md`) but are a **proposed** pricing structure awaiting business sign-off, not a finalized decision - the system enforces whatever numbers are in `PlanTier` today, so changing them later is a data change, not a code change.
- **P12-11** (M) AI credit top-offs: `TOPUP` transactions purchased via the payment provider once `P12-05` lands - the ledger/balance side is already built and ready, this is just wiring a Stripe (or equivalent) checkout flow to create the transaction. Blocked on `P12-05`'s provider decision. `labels: area:api, integration, type:feature`
- **P12-12** (S) Exact token-based AI metering: have `@vaettir/ai-agent`'s functions return real `usage.input_tokens`/`usage.output_tokens` and charge the actual amount instead of `P12-10`'s flat per-operation estimate. Worth doing once real usage data shows whether the estimates are over/under-charging - not blocking anything today. `labels: area:api, type:feature`

### Epic 12.3 - Plan-gated features & data retention
- ✅ **P12-07** (M) Feature-flag-by-tier plumbing: a simple `planTier.key -> Set<featureFlag>` lookup (data, not scattered `if` checks) so specific tiers can gate specific feature sets once those are mapped - deliberately built as an empty, ready-to-fill table now rather than hardcoded later. `labels: area:api, type:architecture` — new `PlanTier.enabledFeatures String[]` (empty by default on every seeded tier) and `packages/core`'s pure `tierHasFeature(tier, flag)`, exposed on `organization.listPlanTiers`. Nothing calls `tierHasFeature` to actually gate anything yet - every feature built this session is available on every tier - this is genuinely just the ready-to-fill lookup the ticket describes, not a retrofit. 3 unit tests.
- 🟡 **P12-08** (L) Data retention enforcement job: a scheduled job that purges/archives data older than `Organization.dataRetentionYears` (default 5) - evidence, audit logs, test results - respecting the higher retention windows compliance-heavy orgs may configure (`P3-08`). This is a real deletion job, not just a documented policy, and needs a dry-run mode and an audit trail of its own before it's trusted to run automatically. `labels: area:api, compliance, type:feature` — dry-run half built (new `services/retentionAudit.ts`, `organization.retentionDryRun`, and a report line on the org settings retention section): counts `AuditLog`/`TestRun`/`TestResultArtifact` rows older than the org's configured cutoff, read-only, no delete/purge code path anywhere. **Deliberately stops there** - the ticket itself calls for a dry-run mode before an actual purge job is trusted to run automatically, and building a real, scheduled, unattended data-deletion capability in the same overnight pass that produced its own dry-run is exactly the kind of destructive, hard-to-reverse work that deserves a human reviewing it before it exists at all, not just before it's turned on. Verified for real: reports 0 against the live org's genuinely-recent data, and correctly counts a synthetic 6-year-old `AuditLog`/`TestRun` row when one is present (inserted and cleaned up as part of verification) - proving the counting logic itself works, not just that it always returns zero. The actual purge logic is still open.
- ✅ **P12-09** (S) Retention policy surfaced in the compliance UI: show an org's actual configured retention window next to the evidence/audit views it governs (`P3-01`), so it isn't a number nobody can see outside the database. `labels: area:web, compliance` — the compliance page's intro line now shows the org's real configured retention (fetched via `project.byId` → `organization.byId`, since retention is org-wide but this view is project-scoped) as a link straight to the org settings page where it's changed. No new API surface needed - `dataRetentionYears` was already exposed by `organization.byId` from `P3-08`.

---

## Phase 13 - Internal Admin & Customer Service

A staff-only surface for Skald & Stone team members - gated to
`@skaldandstone.com` email addresses (or a configured staff allowlist), not
any customer `OrgRole`/`Membership` - to look up accounts, help with support
requests, and handle ownership-level actions that shouldn't live inside a
customer-facing settings page. Distinct from `Phase 12`'s org-scoped admin
UI: that's "an org admin managing their own org," this is "Skald & Stone
staff managing across every org."

- ✅ **P13-01** (M) Staff auth gate: a route tree (e.g. `/admin/**`) restricted to users whose Clerk email matches the configured staff domain/allowlist, checked independently of any `Membership` - a customer `OWNER` role in their own org must not grant access here. `labels: area:api, area:web, type:feature` — new `staffProcedure` in `trpc.ts`, checking the session's real Clerk-verified `User.email` (never client-suppliable) against `STAFF_EMAIL_DOMAIN` (default `skaldandstone.com`) plus an optional `STAFF_EMAIL_ALLOWLIST` for staff on a different domain - independent of `Membership`/`OrgRole` entirely, so a customer `OWNER` gets exactly the same `FORBIDDEN` as anyone else. `/admin` and `/admin/organizations/[id]` are the route tree; the real gate is server-side (every `admin.*` procedure), the pages just surface the resulting error as a plain "staff access required" message rather than duplicating the check client-side.
- ✅ **P13-02** (M) Organization/customer lookup: search and view any organization (name, plan tier, seat usage, member list, creation date) without needing a `Membership` in it - the actual "find this customer" starting point every support request needs. `labels: area:web, area:api, type:feature` — `admin.listOrganizations` (case-insensitive name/slug search) + `admin.getOrganization` (org fields, plan, every member, pending invitations in one call). New `/admin` search page and `/admin/organizations/[id]` detail page.
- ✅ **P13-03** (M) Support actions: the concrete things a support request actually needs done - view (not silently edit) a customer's data for troubleshooting, manually adjust plan tier/seat limits (ahead of `P12-05`'s real billing integration), resend a stuck invite, deactivate a member. `labels: area:api, area:web, type:feature` — `admin.adjustPlanTier` (reuses `changePlanTier`'s exact seat-overflow validation, plus a required `reason`), `admin.resendInvite` (there's no real email dispatch anywhere in this codebase - invitations are token-link based - so "resend" is refreshing the expiry and re-surfacing the same link, verified for real to land exactly 7 days out), `admin.deactivateMember` (same sole-Owner guard as the customer-facing `removeMember`), and `admin.overrideSeatCheck` (a read-only support escape hatch to check seat availability without actually inviting, useful for e.g. Skald & Stone's own known Free-tier seat overage).
- ✅ **P13-04** (S) Admin action audit trail: every staff action taken through this surface writes its own audit record (reusing `AuditLog`'s shape from `P3-06`, but tagged distinctly from a customer's own activity) - "who at Skald & Stone touched this account and why" is a real accountability question independent of the customer's own audit log. `labels: area:api, compliance` — every mutation above calls a new `recordStaffAction` helper, which reuses `recordAudit` (P3-06) but namespaces `entityType` as `"Admin:*"` and stamps `metadata: { source: "staff_admin", reason }` - a customer's own project audit log page never queries that namespace, so staff actions never pollute a customer-facing view, but the record lives in the same append-only, no-update-no-delete place. New `admin.staffActionLog` query + a log section on the org detail page. Verified end-to-end for real against the live local dev database (not just typechecked): a non-staff real user was correctly rejected with `FORBIDDEN`, a throwaway `@skaldandstone.com` user could list/view the real Kall org, a real plan-tier switch-and-revert round-tripped correctly with two matching audit entries, a sole-Owner `deactivateMember` attempt was correctly refused (and correctly did NOT write an audit entry, since nothing actually happened), and a real expired invitation's `resendInvite` landed a new expiry exactly 7.00 days out. All synthetic users/invitations/audit rows deleted after verification.
- ✅ **P13-05** (M) Ownership actions: suspend/reactivate an organization, transfer/reassign an org owner, hard-delete a customer's data - the genuinely dangerous, infrequent actions, gated with real confirmation friction (not a one-click button) and always routed through `P13-04`'s audit trail. `labels: area:api, area:web, type:feature` — all three built. Suspend/reactivate and ownership transfer: new `Organization.suspendedAt`/`suspendedReason`; `trpc.ts`'s `requireNotSuspended` is called from `requireProjectAccess` (every project-scoped router - test cases, plans, releases, everything that's actually the product) and rejects with `FORBIDDEN` regardless of role, while `staffProcedure` itself is deliberately never gated on it, so a suspension can actually be lifted again - reversible by design, changes one timestamp, nothing is deleted. `admin.transferOwnership` is a real two-sided handoff (the new owner's role becomes `OWNER`, the specified previous owner is demoted to `ADMIN`, not removed), guarding against transferring to the same member and against a non-`OWNER` "previous owner" input. Hard-delete - built with James actively available to review, after the earlier pass in this same session had deliberately deferred it as needing exactly that (not unattended overnight work). Started by querying Postgres's own `information_schema` directly for the real FK graph rather than hand-tracing ~40 models from `schema.prisma` - caught a genuine correctness bug before writing any deletion code: `ComplianceFramework`/`ComplianceControl` have no `organizationId`/`projectId` anywhere in the schema at all (shared, platform-wide reference data - even a "custom" framework is visible org-wide), so a naive "delete everything this org's rows reference" approach would have corrupted every OTHER org sharing the same framework. New `services/orgHardDelete.ts`: `previewOrgHardDelete` (read-only per-model counts) and `hardDeleteOrganization` (one transaction, real child-before-parent FK order, explicit per model for auditability even where Postgres would already cascade). The permanent record of a hard-delete can't be a normal `AuditLog` row - that model's `organizationId` is a real, non-cascading FK, so the very audit entry proving the deletion happened would either block the org's own deletion or get destroyed right along with it. New `OrganizationDeletionLog` model instead: a plain snapshot (org id/name/slug, who, why, per-model row counts), never a live FK, so it survives the org it describes. `admin.hardDeleteOrganization` requires typing the org's exact slug as confirmation, checked server-side. New "Permanently delete this organization" flow in the danger zone: preview real counts first, type-to-confirm, native `confirm()`, then a distinct post-deletion success view (the org is gone - can't reload it) showing the deletion record and what was actually removed. Verified about as thoroughly as this session verifies anything: a new `orgHardDelete.integration.test.ts` populates one real row in all 39 reachable models (not a sample - the whole point of this feature is that missing even one either orphans a row forever or throws mid-transaction) in a throwaway org, previews (confirms real non-zero counts for every seeded model), hard-deletes, and confirms zero rows survive anywhere, the deletion log persists with matching counts, and - the specific bug this session caught - the shared `ComplianceFramework`/`ComplianceControl` rows are untouched. Caught and fixed a real bug in the test's own cleanup while building it: `afterAll` doesn't reliably run in Vitest when `beforeAll` throws, so two failed debugging attempts left real (harmless, uniquely-named) orphaned throwaway rows behind - swept up by hand, unrelated to the feature itself, which passed clean on every attempt once the test's own seed data was correct. Then verified again through the actual browser UI end-to-end against a second throwaway org (never Kall, given how destructive this is): preview showed real counts, the confirm button stayed correctly disabled with no input and with a wrong slug typed, enabled only on an exact match, and a real click produced the real "Organization deleted" view with a real deletion-log id - cross-checked directly against Postgres afterward (org gone, log survives with accurate counts and the real staff user's id as `deletedById`). Full `pnpm test` (51 tests) green throughout.

---

## Suggested sequencing

This is a lot of surface area; the phases aren't strictly sequential, but a
reasonable dependency-respecting order is:

1. **Phase 1** (auth/multi-tenancy) unblocks almost everything else safely shipping to real users. `P1-01`/`P1-02`/`P1-03`/`P1-06` are built (email+password auth, JWT sessions, `Membership`-based RBAC, real org/project scoping on every router) - `P1-04` (SSO/SAML), `P1-05` (API keys), `P1-07`–`P1-15` (audit columns, settings UI, CRUD forms, search) are still open.
2. **Phase 2 + Phase 5** (ingestion pipeline + evaluators) in parallel - both are "make the AI/data layer real" work.
3. **Phase 3** (compliance) can start as soon as Phase 1 lands roles - it's largely independent of Phase 2/5/6.
4. **Phase 6** (PR scanning) depends on Phase 5's result-matching to be useful, and feeds Phase 7 directly.
5. **Phase 7** (dashboard) is the payoff phase - hold it until Phase 3/5/6 have real data flowing in, or it'll be a dashboard with nothing to show.
6. **Phase 4** (QA strategy generation) can slot in anytime after Phase 1; it's largely self-contained.
7. **Phase 8/9** (mobile parity, integrations) are pull-based - build them once specific customers/use cases demand them, not speculatively.
8. **Phase 10** (hardening) isn't "last" - P10-01 through P10-04 should start the moment there's real customer data, likely alongside Phase 3.
9. **Phase 11** (migration/import) isn't "last" either, despite the number - `P11-01`/`P11-02` (the import pipeline foundation) and `P11-03` (the TestRail importer) should start as soon as Phase 1's auth/multi-tenancy lands, since customer acquisition depends on painless switching, not on every other phase being done first. The historical-run-backfill piece (`P11-08`) benefits from Phase 5's `TestRun`/`TestResult` ingestion work existing first, but the case/plan importers don't need to wait for it.
10. **Phase 12** (billing/seats) - schema and enforcement logic are already in (see above), so `P12-01`–`P12-03` (seat management UI, invites, read-only UI enforcement) can build directly on the now-real auth/RBAC layer. `P12-05` (payment provider integration) is explicitly blocked on a cost-structure decision, not on engineering sequencing - don't start it until pricing is set. `P12-08` (retention enforcement job) should land before Phase 10's SOC 2 work (`P10-01`) closes out, since "we retain data for 5 years" is itself a control an auditor will ask to see enforced, not just documented.
